apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bshop-api.fullname" . }}-test-connection"
  labels:
    {{- include "bshop-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bshop-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
