# Default values for identity-api.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# helm package .
# helm lint -f ./values.yaml .
# helm upgrade identity-api --install -f ./values.yaml --namespace=bshop .

replicaCount: 1

image:
  repository: identity-api
  tag: latest
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: identity-api
fullnameOverride: identity-api

env:
  ASPNETCORE_ENVIRONMENT: Production
  ASPNETCORE_URLS: http://0.0.0.0:80
  PathBase: /identity
  ConnectionStrings__DefaultConnection: Server=mssql-mssql-linux,1433;Database=identity-api;User Id=sa;Password=P@ssw0rd;
  Client__Swagger__Uri: http://swagger
  Client__React__Uri: http://react-web

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths: []
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  limits:
    cpu: 100m
    memory: 256Mi
  requests:
    cpu: 50m
    memory: 128Mi

probes:
  liveness:
    path: /health
    initialDelaySeconds: 15
    periodSeconds: 30
  readiness:
    path: /health
    initialDelaySeconds: 30
    periodSeconds: 60
    timeoutSeconds: 5

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
