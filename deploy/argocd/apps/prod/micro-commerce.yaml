apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: micro-commerce-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/baotoq/micro-commerce"
    path: deploy/apps/prod
    targetRevision: master
  destination:
    server: "https://kubernetes.default.svc"
    namespace: argocd-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - PruneLast=true
      - CreateNamespace=true
