---
phase: 24-infrastructure-manifests-and-secrets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/k8s/kind-config.yaml
  - infra/k8s/base/kustomization.yaml
  - infra/k8s/base/namespace.yaml
  - infra/k8s/base/postgres/kustomization.yaml
  - infra/k8s/base/postgres/statefulset.yaml
  - infra/k8s/base/postgres/service.yaml
  - infra/k8s/overlays/dev/kustomization.yaml
autonomous: true
requirements: [INFRA-04, INFRA-01]

must_haves:
  truths:
    - "kind-config.yaml defines a single control-plane node with port mappings for PostgreSQL (35432), RabbitMQ (35672), and Keycloak (38080)"
    - "PostgreSQL runs as a StatefulSet with a 1Gi PersistentVolumeClaim"
    - "PostgreSQL uses PGDATA subdirectory to avoid initdb 'directory not empty' error"
    - "Kustomize base kustomization.yaml references namespace, postgres, rabbitmq, and keycloak subdirectories"
    - "Dev overlay exists and references the base"
  artifacts:
    - path: "infra/k8s/kind-config.yaml"
      provides: "kind cluster configuration with port mappings"
      contains: "extraPortMappings"
    - path: "infra/k8s/base/kustomization.yaml"
      provides: "Root Kustomize base referencing all infrastructure"
      contains: "resources"
    - path: "infra/k8s/base/namespace.yaml"
      provides: "micro-commerce namespace"
      contains: "micro-commerce"
    - path: "infra/k8s/base/postgres/statefulset.yaml"
      provides: "PostgreSQL StatefulSet with PVC"
      contains: "volumeClaimTemplates"
    - path: "infra/k8s/base/postgres/service.yaml"
      provides: "Headless + NodePort services for PostgreSQL"
      contains: "nodePort: 30432"
    - path: "infra/k8s/overlays/dev/kustomization.yaml"
      provides: "Dev overlay referencing base"
      contains: "../../base"
  key_links:
    - from: "infra/k8s/kind-config.yaml"
      to: "infra/k8s/base/postgres/service.yaml"
      via: "Port mapping alignment"
      pattern: "containerPort: 30432.*nodePort: 30432"
    - from: "infra/k8s/base/postgres/statefulset.yaml"
      to: "infra/k8s/base/postgres/service.yaml"
      via: "Headless service required by StatefulSet"
      pattern: "serviceName: postgres"
---

<objective>
Create the kind cluster configuration, Kustomize directory scaffold, and PostgreSQL manifests for the MicroCommerce Kubernetes infrastructure.

Purpose: Establish the foundation directory structure (`infra/k8s/`) with kind cluster config, Kustomize base/overlay structure, and the first stateful infrastructure service (PostgreSQL with PVC-backed StatefulSet).
Output: kind-config.yaml with offset port mappings, complete Kustomize scaffold, PostgreSQL StatefulSet + headless Service + NodePort Service + kustomization.yaml, dev overlay skeleton.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-infrastructure-manifests-and-secrets/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create kind cluster config and Kustomize directory scaffold</name>
  <files>infra/k8s/kind-config.yaml, infra/k8s/base/kustomization.yaml, infra/k8s/base/namespace.yaml, infra/k8s/overlays/dev/kustomization.yaml</files>
  <action>
Create the `infra/k8s/` directory structure with four files.

**1. `infra/k8s/kind-config.yaml`** — kind cluster configuration:
```yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: micro-commerce
nodes:
- role: control-plane
  extraPortMappings:
  # PostgreSQL (NodePort 30432 -> host 35432)
  - containerPort: 30432
    hostPort: 35432
    protocol: TCP
  # RabbitMQ Management UI (NodePort 30672 -> host 35672)
  - containerPort: 30672
    hostPort: 35672
    protocol: TCP
  # Keycloak (NodePort 30080 -> host 38080)
  - containerPort: 30080
    hostPort: 38080
    protocol: TCP
```

Key decisions per user context:
- Single control-plane node only (no separate workers)
- Fixed cluster name `micro-commerce`
- Offset ports (3xxxx) to avoid conflicts with Aspire local dev services
- Three port mappings: PostgreSQL 35432, RabbitMQ management 35672, Keycloak 38080

**2. `infra/k8s/base/kustomization.yaml`** — Root Kustomize base:
```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: micro-commerce

resources:
- namespace.yaml
- postgres/
- rabbitmq/
- keycloak/
```

The `namespace` field ensures all resources are placed in `micro-commerce` even if individual manifests omit it.

**3. `infra/k8s/base/namespace.yaml`** — Namespace manifest:
```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: micro-commerce
```

**4. `infra/k8s/overlays/dev/kustomization.yaml`** — Dev overlay (references base, placeholder for future patches):
```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base
```

Create the directory structure by creating these 4 files. The `base/postgres/`, `base/rabbitmq/`, and `base/keycloak/` subdirectories will be populated by this plan (Task 2 for postgres) and Plan 02 (rabbitmq + keycloak).
  </action>
  <verify>
    <automated>test -f infra/k8s/kind-config.yaml && test -f infra/k8s/base/kustomization.yaml && test -f infra/k8s/base/namespace.yaml && test -f infra/k8s/overlays/dev/kustomization.yaml && grep -q "micro-commerce" infra/k8s/kind-config.yaml && grep -q "extraPortMappings" infra/k8s/kind-config.yaml && grep -q "30432" infra/k8s/kind-config.yaml && grep -q "30672" infra/k8s/kind-config.yaml && grep -q "30080" infra/k8s/kind-config.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>kind-config.yaml exists with 3 port mappings (35432/35672/38080), Kustomize base has root kustomization referencing namespace + 3 subdirectories, namespace.yaml defines micro-commerce, dev overlay references base</done>
</task>

<task type="auto">
  <name>Task 2: Create PostgreSQL StatefulSet and Service manifests</name>
  <files>infra/k8s/base/postgres/kustomization.yaml, infra/k8s/base/postgres/statefulset.yaml, infra/k8s/base/postgres/service.yaml</files>
  <action>
Create PostgreSQL Kubernetes manifests in `infra/k8s/base/postgres/`.

**1. `infra/k8s/base/postgres/kustomization.yaml`:**
```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- statefulset.yaml
- service.yaml
- sealed-secret.yaml
```

Note: `sealed-secret.yaml` does not exist yet -- it will be generated by the bootstrap script (Plan 03). Include it in the resource list now so the kustomization is complete. The bootstrap script generates it before `kubectl apply -k`.

**2. `infra/k8s/base/postgres/statefulset.yaml`:**
```yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: micro-commerce
spec:
  serviceName: postgres
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:17
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: postgres-credentials
              key: username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-credentials
              key: password
        - name: POSTGRES_DB
          value: appdb
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "postgres"]
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          exec:
            command: ["pg_isready", "-U", "postgres"]
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            memory: 256Mi
            cpu: 250m
          limits:
            memory: 512Mi
            cpu: 500m
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi
```

Key decisions:
- `PGDATA=/var/lib/postgresql/data/pgdata` — subdirectory avoids "initdb: directory is not empty" error from kind's local-path-provisioner (which may leave `lost+found` in the mount root).
- Credentials from `postgres-credentials` Secret (will be sealed by bootstrap script).
- Database name `appdb` matches the Aspire dev database name.
- 1 replica for dev cluster.
- Resource limits: 256Mi-512Mi memory, 250m-500m CPU (reasonable for dev).
- kind's default `standard` StorageClass (local-path-provisioner) handles PVC automatically.

**3. `infra/k8s/base/postgres/service.yaml`** — Two services (headless for StatefulSet + NodePort for local access):
```yaml
# Headless service for StatefulSet (required for stable DNS)
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: micro-commerce
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
---
# NodePort for local access via kind port mapping
apiVersion: v1
kind: Service
metadata:
  name: postgres-nodeport
  namespace: micro-commerce
spec:
  type: NodePort
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
    nodePort: 30432
```

Key decisions:
- Headless Service (`clusterIP: None`) is required by StatefulSet for stable DNS names (`postgres-0.postgres.micro-commerce.svc.cluster.local`).
- Separate NodePort service with `nodePort: 30432` — this must match kind-config.yaml's `containerPort: 30432` for the port mapping chain to work (host 35432 -> kind container 30432 -> K8s NodePort 30432 -> pod 5432).
  </action>
  <verify>
    <automated>test -f infra/k8s/base/postgres/kustomization.yaml && test -f infra/k8s/base/postgres/statefulset.yaml && test -f infra/k8s/base/postgres/service.yaml && grep -q "volumeClaimTemplates" infra/k8s/base/postgres/statefulset.yaml && grep -q "PGDATA" infra/k8s/base/postgres/statefulset.yaml && grep -q "clusterIP: None" infra/k8s/base/postgres/service.yaml && grep -q "nodePort: 30432" infra/k8s/base/postgres/service.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>PostgreSQL StatefulSet has PVC (1Gi), PGDATA subdirectory, credentials from Secret, readiness/liveness probes. Headless Service exists for StatefulSet. NodePort Service exposes port 30432 matching kind-config.yaml.</done>
</task>

</tasks>

<verification>
- `infra/k8s/kind-config.yaml` exists with cluster name `micro-commerce`, single control-plane node, 3 port mappings
- `infra/k8s/base/kustomization.yaml` references namespace.yaml, postgres/, rabbitmq/, keycloak/
- `infra/k8s/base/namespace.yaml` defines `micro-commerce` namespace
- `infra/k8s/overlays/dev/kustomization.yaml` references `../../base`
- `infra/k8s/base/postgres/statefulset.yaml` has StatefulSet with volumeClaimTemplates, PGDATA env var, secretKeyRef for credentials
- `infra/k8s/base/postgres/service.yaml` has headless Service (clusterIP: None) and NodePort Service (nodePort: 30432)
- Port mapping chain is aligned: kind hostPort 35432 -> containerPort 30432 -> K8s nodePort 30432 -> pod 5432
</verification>

<success_criteria>
- Kind cluster config supports PostgreSQL, RabbitMQ, and Keycloak port access from localhost
- PostgreSQL StatefulSet uses PVC for data persistence and PGDATA subdirectory
- Kustomize base/overlay structure is in place with all subdirectories referenced
- All manifests are namespaced under micro-commerce
- No plaintext credentials in any manifest (credentials reference a Secret)
</success_criteria>

<output>
After completion, create `.planning/phases/24-infrastructure-manifests-and-secrets/24-01-SUMMARY.md`
</output>
