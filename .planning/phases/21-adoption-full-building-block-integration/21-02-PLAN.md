---
phase: 21-adoption-full-building-block-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/MicroCommerce.ApiService/Features/Cart/Domain/Entities/Cart.cs
  - src/MicroCommerce.ApiService/Features/Ordering/Domain/Entities/Order.cs
  - src/MicroCommerce.ApiService/Features/Inventory/Domain/Entities/StockItem.cs
  - src/MicroCommerce.ApiService/Features/Profiles/Domain/Entities/UserProfile.cs
  - src/MicroCommerce.ApiService/Features/Reviews/Domain/Entities/Review.cs
  - src/MicroCommerce.ApiService/Features/Wishlists/Domain/Entities/WishlistItem.cs
autonomous: true
requirements:
  - ADOPT-03

must_haves:
  truths:
    - "All 6 entities with [Timestamp] uint Version are migrated to IConcurrencyToken with int Version"
    - "EF Core migrations generated for all 6 affected DbContexts (Cart, Ordering, Inventory, Profiles, Reviews, Wishlists)"
    - "ConcurrencyTokenConvention automatically marks Version as concurrency token (no manual [ConcurrencyCheck] needed)"
    - "ConcurrencyInterceptor auto-increments Version on save (no manual version management)"
    - "All existing tests pass after migration"
  artifacts:
    - path: "src/MicroCommerce.ApiService/Features/Cart/Domain/Entities/Cart.cs"
      provides: "Cart implementing IConcurrencyToken"
      contains: "IConcurrencyToken"
    - path: "src/MicroCommerce.ApiService/Features/Ordering/Domain/Entities/Order.cs"
      provides: "Order implementing IConcurrencyToken"
      contains: "IConcurrencyToken"
    - path: "src/MicroCommerce.ApiService/Features/Inventory/Domain/Entities/StockItem.cs"
      provides: "StockItem implementing IConcurrencyToken"
      contains: "IConcurrencyToken"
  key_links:
    - from: "src/MicroCommerce.ApiService/Features/Cart/Domain/Entities/Cart.cs"
      to: "src/BuildingBlocks/BuildingBlocks.Common/IConcurrencyToken.cs"
      via: "interface implementation"
      pattern: "IConcurrencyToken"
    - from: "src/MicroCommerce.ApiService/Common/Persistence/Conventions/ConcurrencyTokenConvention.cs"
      to: "IConcurrencyToken entities"
      via: "convention auto-detects IConcurrencyToken and marks Version"
      pattern: "IConcurrencyToken"
---

<objective>
Migrate all 6 entities with PostgreSQL xmin-based optimistic concurrency to explicit IConcurrencyToken with int Version column, and generate EF Core migrations for the schema changes.

Purpose: Replace PostgreSQL-specific xmin system column with portable explicit Version column that works with the ConcurrencyInterceptor (auto-increments on save) and ConcurrencyTokenConvention (auto-marks as concurrency token). This completes ADOPT-03.

Output: 6 entities implement IConcurrencyToken; [Timestamp] uint Version replaced with int Version { get; set; }; EF migrations generated for 6 DbContexts; all tests pass.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-adoption-full-building-block-integration/21-RESEARCH.md
@.planning/phases/21-adoption-full-building-block-integration/21-01-SUMMARY.md
@src/BuildingBlocks/BuildingBlocks.Common/IConcurrencyToken.cs
@src/MicroCommerce.ApiService/Common/Persistence/Conventions/ConcurrencyTokenConvention.cs
@src/MicroCommerce.ApiService/Common/Persistence/ConcurrencyInterceptor.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 6 entities from xmin [Timestamp] uint to IConcurrencyToken int Version</name>
  <files>
    src/MicroCommerce.ApiService/Features/Cart/Domain/Entities/Cart.cs
    src/MicroCommerce.ApiService/Features/Ordering/Domain/Entities/Order.cs
    src/MicroCommerce.ApiService/Features/Inventory/Domain/Entities/StockItem.cs
    src/MicroCommerce.ApiService/Features/Profiles/Domain/Entities/UserProfile.cs
    src/MicroCommerce.ApiService/Features/Reviews/Domain/Entities/Review.cs
    src/MicroCommerce.ApiService/Features/Wishlists/Domain/Entities/WishlistItem.cs
  </files>
  <action>
For each of the 6 entities with `[Timestamp] public uint Version`, apply the IConcurrencyToken migration:

**For each entity (Cart, Order, StockItem, UserProfile, Review, WishlistItem):**

1. Remove `using System.ComponentModel.DataAnnotations;` if [Timestamp] was the only reason for the import (check if other DataAnnotations are used — if not, remove the using)
2. Add IConcurrencyToken to class declaration. For entities already implementing interfaces, append it:
   - `Cart : AuditableAggregateRoot<CartId>, IConcurrencyToken` (after Plan 01 it's AuditableAggregateRoot)
   - `Order : BaseAggregateRoot<OrderId>, IConcurrencyToken`
   - `StockItem : BaseAggregateRoot<StockItemId>, IConcurrencyToken`
   - `UserProfile : AuditableAggregateRoot<UserProfileId>, IConcurrencyToken` (after Plan 01)
   - `Review : AuditableAggregateRoot<ReviewId>, IConcurrencyToken` (after Plan 01)
   - `WishlistItem : Entity<WishlistItemId>, IConcurrencyToken` (WishlistItem is NOT an aggregate root. After Plan 01 it may or may not inherit Entity — check. If it currently has no base, add Entity<WishlistItemId> per ADOPT-01 scope. WishlistItem was NOT in the Plan 01 child entity list because it has an Id but is a standalone entity, not a child. For this plan, just add IConcurrencyToken to whatever the current declaration is.)

   NOTE: WishlistItem is a standalone entity, not a child of an aggregate. It currently has `public sealed class WishlistItem` with no base class. For consistency, add `Entity<WishlistItemId>` as the base class (same migration as ADOPT-01 entities): change `private WishlistItem()` to `protected WishlistItem() : base() { }`, add `private WishlistItem(WishlistItemId id) : base(id) { }`, remove `public WishlistItemId Id { get; private set; }`, update Create to use `new WishlistItem(WishlistItemId.New())`.

3. Replace the Version property:
   ```csharp
   // Remove these lines:
   [Timestamp]
   public uint Version { get; private set; }

   // Replace with:
   public int Version { get; set; }  // IConcurrencyToken requires { get; set; }
   ```

4. Remove the XML comment about "xmin system column" if present.

5. Ensure `using MicroCommerce.BuildingBlocks.Common;` is present (for IConcurrencyToken).

**IMPORTANT: After changes, search for `.Version` in test files:**
- `grep -rn "\.Version" src/MicroCommerce.ApiService.Tests/` — update any assertions that reference `uint` Version or check specific values. The type change from `uint` to `int` should not break most assertions since both default to 0.
- If any EF Core configuration files have explicit `IsRowVersion()` or `[Timestamp]` mappings, remove them — the ConcurrencyTokenConvention handles it automatically.

Check EF Core entity configurations for any explicit xmin/Version configuration and remove them:
- Search: `grep -rn "IsRowVersion\|IsConcurrencyToken\|xmin" src/MicroCommerce.ApiService/Features/*/Infrastructure/Configurations/`
- Remove any found mappings since ConcurrencyTokenConvention handles IConcurrencyToken entities automatically.

Do NOT remove the CheckoutState xmin configuration (MassTransit saga is NOT IConcurrencyToken — per Phase 16 decision).
  </action>
  <verify>
    <automated>dotnet build /Users/baotoq/Work/micro-commerce/src/MicroCommerce.ApiService/MicroCommerce.ApiService.csproj --no-restore 2>&amp;1 | tail -5</automated>
    <manual>Verify all 6 entities implement IConcurrencyToken; no [Timestamp] attributes remain except on CheckoutState</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All 6 entities (Cart, Order, StockItem, UserProfile, Review, WishlistItem) implement IConcurrencyToken with `int Version { get; set; }`; no [Timestamp] attributes remain on these entities; WishlistItem also inherits Entity&lt;WishlistItemId&gt;; code compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Generate EF Core migrations for IConcurrencyToken schema changes</name>
  <files>
    src/MicroCommerce.ApiService/Features/Cart/Infrastructure/Migrations/
    src/MicroCommerce.ApiService/Features/Ordering/Infrastructure/Migrations/
    src/MicroCommerce.ApiService/Features/Inventory/Infrastructure/Migrations/
    src/MicroCommerce.ApiService/Features/Profiles/Infrastructure/Migrations/
    src/MicroCommerce.ApiService/Features/Reviews/Infrastructure/Migrations/
    src/MicroCommerce.ApiService/Features/Wishlists/Infrastructure/Migrations/
  </files>
  <action>
Generate EF Core migrations for each of the 6 affected DbContexts. The migration adds a real `version` integer column (snake_case per naming convention) and removes the xmin system column mapping.

Run these commands from the project root:

```bash
cd /Users/baotoq/Work/micro-commerce

# Cart
dotnet ef migrations add AddExplicitVersionColumn \
  --project src/MicroCommerce.ApiService \
  --context CartDbContext

# Ordering
dotnet ef migrations add AddExplicitVersionColumn \
  --project src/MicroCommerce.ApiService \
  --context OrderingDbContext

# Inventory
dotnet ef migrations add AddExplicitVersionColumn \
  --project src/MicroCommerce.ApiService \
  --context InventoryDbContext

# Profiles
dotnet ef migrations add AddExplicitVersionColumn \
  --project src/MicroCommerce.ApiService \
  --context ProfilesDbContext

# Reviews
dotnet ef migrations add AddExplicitVersionColumn \
  --project src/MicroCommerce.ApiService \
  --context ReviewsDbContext

# Wishlists
dotnet ef migrations add AddExplicitVersionColumn \
  --project src/MicroCommerce.ApiService \
  --context WishlistsDbContext
```

After generating migrations:
1. Inspect each migration to verify it adds an integer `version` column and removes the xmin mapping
2. The migration SQL should include: `ALTER TABLE {schema}.{table} ADD COLUMN version INTEGER NOT NULL DEFAULT 0;`
3. If the generated migration also includes AuditableAggregateRoot changes (nullable UpdatedAt becoming non-nullable from Plan 01), that is expected and correct — the migration captures ALL pending model changes

Also generate migrations for the AuditableAggregateRoot changes from Plan 01 if they were not captured there. The following schema changes may be included:
- Category/Product: `updated_at` column changes from `NULLABLE timestamp with time zone` to `NOT NULL timestamp with time zone` with default `'-infinity'`
- Cart: `last_modified_at` column renamed to `updated_at` (via RenameColumn or drop+add depending on provider)

Run full test suite to verify no regressions:
```bash
dotnet test src/MicroCommerce.ApiService.Tests/
```

NOTE: Integration tests use per-schema MigrateAsync which will apply these new migrations automatically. If integration tests fail due to schema mismatch, the migrations are working correctly — the test infrastructure handles it.
  </action>
  <verify>
    <automated>dotnet test /Users/baotoq/Work/micro-commerce/src/MicroCommerce.ApiService.Tests/ 2>&amp;1 | tail -10</automated>
    <manual>Verify 6 new migration files exist (one per DbContext) with AddExplicitVersionColumn name</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>EF Core migrations generated for all 6 DbContexts; each migration adds integer version column and removes xmin mapping; all tests pass including integration tests that apply migrations.</done>
</task>

</tasks>

<verification>
1. `grep -rn "\[Timestamp\]" src/MicroCommerce.ApiService/Features/` — only CheckoutState should have [Timestamp]
2. `grep -rn "IConcurrencyToken" src/MicroCommerce.ApiService/Features/` — 6 entities implement it
3. `dotnet test src/MicroCommerce.ApiService.Tests/` — all tests pass
4. Migration files exist in each of the 6 DbContext migration directories
</verification>

<success_criteria>
- 6 entities implement IConcurrencyToken with int Version { get; set; }
- No [Timestamp] attributes on Cart, Order, StockItem, UserProfile, Review, WishlistItem
- 6 EF Core migrations generated (one per DbContext)
- All tests pass (unit + integration)
</success_criteria>

<output>
After completion, create `.planning/phases/21-adoption-full-building-block-integration/21-02-SUMMARY.md`
</output>
