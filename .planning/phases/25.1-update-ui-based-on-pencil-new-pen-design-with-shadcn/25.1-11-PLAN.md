---
phase: 25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn
plan: 11
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - src/MicroCommerce.Web/src/app/admin/layout.tsx
  - src/MicroCommerce.Web/src/app/admin/page.tsx
  - src/MicroCommerce.Web/src/components/admin/products-table.tsx
  - src/MicroCommerce.Web/src/components/admin/order-dashboard.tsx
  - src/MicroCommerce.Web/src/components/admin/order-kanban.tsx
  - src/MicroCommerce.Web/src/components/admin/order-kanban-column.tsx
  - src/MicroCommerce.Web/src/components/admin/order-kanban-card.tsx
  - src/MicroCommerce.Web/src/components/admin/pagination.tsx
  - src/MicroCommerce.Web/src/components/admin/product-filters.tsx
  - src/MicroCommerce.Web/src/components/admin/product-drawer.tsx
  - src/MicroCommerce.Web/src/components/ui/sidebar.tsx
autonomous: true
requirements:
  - UI-ADMIN
must_haves:
  truths:
    - "Admin layout uses dark navy sidebar instead of top navigation"
    - "Sidebar has active state highlighting with --sidebar-accent color"
    - "Admin dashboard shows summary stats cards"
    - "Products table displays with proper styling"
    - "Order kanban/dashboard renders correctly"
    - "Sidebar collapses to mobile sheet on small screens"
  artifacts:
    - path: "src/MicroCommerce.Web/src/components/ui/sidebar.tsx"
      provides: "shadcn sidebar component"
    - path: "src/MicroCommerce.Web/src/app/admin/layout.tsx"
      provides: "Rewritten admin layout with dark sidebar"
    - path: "src/MicroCommerce.Web/src/app/admin/page.tsx"
      provides: "Restyled admin dashboard with stat cards"
  key_links:
    - from: "src/MicroCommerce.Web/src/app/admin/layout.tsx"
      to: "src/MicroCommerce.Web/src/components/ui/sidebar.tsx"
      via: "SidebarProvider + Sidebar for persistent nav"
      pattern: "SidebarProvider"
    - from: "src/MicroCommerce.Web/src/app/admin/layout.tsx"
      to: "src/MicroCommerce.Web/src/app/globals.css"
      via: "CSS variable overrides for dark sidebar (scoped to admin wrapper)"
      pattern: "sidebar"
---

<objective>
Replace the admin top navigation with a dark navy sidebar layout using shadcn Sidebar, and restyle dashboard stat cards, product table, and order views matching design/admin-dashboard.pen.

Purpose: Admin layout switch from top-nav to sidebar is the largest structural change. Dark navy sidebar provides clear visual separation from the storefront.
Output: Installed shadcn sidebar, rewritten admin layout, restyled admin components.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-CONTEXT.md
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-RESEARCH.md
@design/admin-dashboard.pen
@design/design-system.pen
@src/MicroCommerce.Web/src/app/admin/layout.tsx
@src/MicroCommerce.Web/src/app/admin/page.tsx
@src/MicroCommerce.Web/src/components/admin/products-table.tsx
@src/MicroCommerce.Web/src/components/admin/order-dashboard.tsx
@src/MicroCommerce.Web/src/components/admin/order-kanban.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn sidebar and rewrite admin layout with dark nav</name>
  <files>
    src/MicroCommerce.Web/src/components/ui/sidebar.tsx,
    src/MicroCommerce.Web/src/app/admin/layout.tsx
  </files>
  <action>
1. **Install shadcn sidebar** (run from src/MicroCommerce.Web):
   ```
   npx shadcn@latest add sidebar
   ```

2. **Rewrite admin/layout.tsx**: Read design/admin-dashboard.pen sidebar section. Replace current top-nav layout entirely:

   - Import SidebarProvider, Sidebar, SidebarContent, SidebarGroup, SidebarGroupLabel, SidebarMenu, SidebarMenuItem, SidebarMenuButton from `@/components/ui/sidebar`
   - Wrap in a div with scoped CSS variable overrides for dark sidebar:
     ```
     style={{
       "--sidebar": "oklch(0.178 0.02 256.788)",          // #111827
       "--sidebar-foreground": "oklch(0.985 0.002 247.839)", // #F9FAFB
       "--sidebar-accent": "oklch(0.237 0.02 256.788)",     // #1F2937
       "--sidebar-accent-foreground": "oklch(0.985 0.002 247.839)",
       "--sidebar-primary": "oklch(0.546 0.245 262.881)",   // #2563EB
       "--sidebar-primary-foreground": "oklch(1 0 0)",
       "--sidebar-border": "oklch(0.295 0.02 256.788)",     // #374151
     } as React.CSSProperties}
     ```
   - SidebarProvider wrapping Sidebar + main content
   - Sidebar content:
     - Logo/brand: "MicroCommerce Admin" at top
     - Nav groups with icons matching current links: Dashboard (LayoutDashboard), Products (Package), Categories (FolderTree), Orders (ShoppingBag), Dead Letters (AlertTriangle)
     - Each SidebarMenuItem with SidebarMenuButton wrapping Link components
     - Active state detection via `usePathname()` — set `isActive` prop on SidebarMenuButton
   - Main content area: `<main className="flex-1 overflow-auto">{children}</main>`
   - Keep QueryProvider and Toaster wrappers
   - Add SidebarTrigger button in main area header for mobile toggle

PITFALL AVOIDANCE (from RESEARCH.md Pitfall 4): Scope sidebar CSS variables to the admin layout wrapper element ONLY. Do NOT put them in `:root` or globals.css. Use inline style on the admin wrapper div. This prevents storefront sidebar/sheet from picking up dark colors.

CRITICAL: Preserve all existing admin routes and nav links. Only change the layout structure from top-nav to sidebar.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && test -f src/components/ui/sidebar.tsx && echo "PASS: Sidebar installed" && grep -q "SidebarProvider" src/app/admin/layout.tsx && echo "PASS: SidebarProvider used" && grep -q "#111827\|0.178" src/app/admin/layout.tsx && echo "PASS: Dark sidebar colors scoped" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>shadcn sidebar installed. Admin layout rewritten with dark navy sidebar, icon+label nav items, active state highlighting, mobile sheet collapse. Sidebar CSS variables scoped to admin only.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle admin dashboard page and key admin components</name>
  <files>
    src/MicroCommerce.Web/src/app/admin/page.tsx,
    src/MicroCommerce.Web/src/components/admin/products-table.tsx,
    src/MicroCommerce.Web/src/components/admin/order-dashboard.tsx,
    src/MicroCommerce.Web/src/components/admin/order-kanban.tsx,
    src/MicroCommerce.Web/src/components/admin/order-kanban-column.tsx,
    src/MicroCommerce.Web/src/components/admin/order-kanban-card.tsx,
    src/MicroCommerce.Web/src/components/admin/pagination.tsx,
    src/MicroCommerce.Web/src/components/admin/product-filters.tsx,
    src/MicroCommerce.Web/src/components/admin/product-drawer.tsx
  </files>
  <action>
1. **Restyle admin/page.tsx (dashboard)**: Read design/admin-dashboard.pen main content area.
   - Summary stat cards at top: Total Products, Total Orders, Revenue, Active Users (or similar metrics)
   - Each stat card: icon, label, value, change indicator (up/down arrow with percentage)
   - Use shadcn Card for stat cards in a responsive grid (4 cols on desktop, 2 on tablet, 1 on mobile)
   - Recent orders section below (mini table or list)
   - Keep existing data fetching unchanged

2. **Restyle products-table.tsx**:
   - Use shadcn Table with proper header row styling
   - Columns: Image thumbnail, Name, Category, Price, Stock, Status, Actions
   - Status: Badge with appropriate color (Active=success, Draft=warning, etc.)
   - Actions: Edit button (opens ProductDrawer), Delete button
   - Keep existing table data, sort, filter logic unchanged

3. **Restyle admin pagination.tsx**: Use the shadcn Pagination pattern (or update to match design).

4. **Restyle admin product-filters.tsx**: Consistent with storefront filter styling but simpler (search + category dropdown).

5. **Restyle product-drawer.tsx**: Update shadcn Sheet/Drawer styling for product create/edit form.

6. **Restyle order components** (order-dashboard, order-kanban, order-kanban-column, order-kanban-card):
   - Order kanban: columns for each order status (Pending, Processing, Shipped, Delivered)
   - Kanban cards: compact card showing order #, customer, total, status badge
   - Keep existing drag-and-drop or status update logic unchanged
   - Order dashboard: summary view with key metrics

CRITICAL: Keep all data fetching hooks, CRUD operations, and state management unchanged. Only restyle JSX/classes.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && grep -q "Card" src/app/admin/page.tsx && echo "PASS: Stat cards in dashboard" && grep -q "Table" src/components/admin/products-table.tsx && echo "PASS: Table in products" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Admin dashboard restyled with stat cards grid. Products table, pagination, filters, and order kanban components restyled. All existing functionality preserved.</done>
</task>

</tasks>

<verification>
- Admin layout shows dark navy sidebar instead of top nav
- Sidebar nav items match current routes with active state
- Dashboard shows stat cards in responsive grid
- Products table uses shadcn Table with status badges
- Order kanban renders with styled columns and cards
- Sidebar collapses to mobile sheet
- `npm run lint` passes
</verification>

<success_criteria>
1. shadcn sidebar component installed
2. Admin layout uses dark sidebar (--sidebar: #111827) scoped to admin wrapper
3. Sidebar has icon+label nav for all admin routes with active highlighting
4. Dashboard page shows stat cards with icons and values
5. Products table uses shadcn Table with styled rows and badges
6. Order kanban cards have status badges with semantic colors
7. Sidebar CSS variables do NOT bleed into storefront
8. Biome lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-11-SUMMARY.md`
</output>
