---
phase: 25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn
plan: 10
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - src/MicroCommerce.Web/src/app/auth/login/page.tsx
autonomous: true
requirements:
  - UI-AUTH-LOGIN
must_haves:
  truths:
    - "Login page shows a branded login form with MicroCommerce identity"
    - "Sign In button triggers Keycloak authentication flow"
    - "Page is visually consistent with the storefront design theme"
  artifacts:
    - path: "src/MicroCommerce.Web/src/app/auth/login/page.tsx"
      provides: "Branded login page wrapping Keycloak auth redirect"
  key_links:
    - from: "src/MicroCommerce.Web/src/app/auth/login/page.tsx"
      to: "src/MicroCommerce.Web/src/auth.ts"
      via: "signIn('keycloak') call for SSO redirect"
      pattern: "signIn"
---

<objective>
Create a branded login page matching design/auth-login.pen that wraps the Keycloak SSO redirect with the storefront's visual identity.

Purpose: Login page provides brand consistency instead of a raw Keycloak redirect. Simple wrapper, not a full custom auth flow.
Output: New auth/login/page.tsx with branded form that triggers signIn("keycloak").
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-CONTEXT.md
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-RESEARCH.md
@design/auth-login.pen
@design/design-system.pen
@src/MicroCommerce.Web/src/auth.ts
@src/MicroCommerce.Web/src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branded auth login page</name>
  <files>
    src/MicroCommerce.Web/src/app/auth/login/page.tsx
  </files>
  <action>
Read design/auth-login.pen for the login page layout. Create (or rewrite if exists) `src/app/auth/login/page.tsx`:

1. **Layout**: Centered card on a subtle background
   - Full-page centered container: `min-h-screen flex items-center justify-center bg-secondary`
   - Login card: `max-w-md w-full` using shadcn Card

2. **Card content**:
   - Logo/brand: "MicroCommerce" text or icon at top
   - Heading: "Welcome Back" or "Sign In"
   - Subtext: "Sign in to access your account"
   - "Sign in with Keycloak" button (primary variant, full width)
     - Use a server action or client-side `signIn("keycloak")` from next-auth/react
   - Optional: "Continue as Guest" link back to homepage

3. **If design shows email/password fields**: Include styled inputs (email, password) with a "Sign In" button. However, since auth is handled by Keycloak SSO, the primary action should be the Keycloak redirect. If the design shows a form, treat it as visual chrome around the SSO button.

4. **Implementation approach**:
   - If this is a new file: "use client" directive, import `signIn` from `next-auth/react`
   - If an existing file exists at this path: rewrite the JSX to match design, keep existing auth logic
   - Handle potential callback URL from query params for post-login redirect

5. **Styling**: Use shadcn Button, Card, Input, Label. Keep consistent with theme (primary blue, DM Sans font).

NOTE: Check if `src/app/auth/login/page.tsx` already exists. If not, create the `auth/login/` directory structure first. If middleware redirects to a different auth path, adapt the path accordingly.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && test -f src/app/auth/login/page.tsx && echo "PASS: Login page exists" && grep -q "signIn\|sign-in\|Keycloak\|keycloak" src/app/auth/login/page.tsx && echo "PASS: Auth trigger present" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Branded login page created at auth/login with Keycloak SSO button, branded card layout, consistent theme styling. Auth flow unchanged.</done>
</task>

</tasks>

<verification>
- Login page renders at /auth/login with branded card
- Sign In button triggers Keycloak auth flow
- Page uses the design theme (primary blue, DM Sans font)
- `npm run lint` passes
</verification>

<success_criteria>
1. Login page exists at src/app/auth/login/page.tsx
2. Page shows branded card with sign-in action
3. Keycloak authentication is triggered via button click
4. Visual consistency with storefront theme
5. Biome lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-10-SUMMARY.md`
</output>
