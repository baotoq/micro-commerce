---
phase: 25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/MicroCommerce.Web/src/app/globals.css
  - src/MicroCommerce.Web/src/app/layout.tsx
autonomous: true
requirements:
  - UI-THEME
must_haves:
  truths:
    - "All pages render with DM Sans font instead of Geist Sans"
    - "Primary color is blue (#2563EB) not grayscale"
    - "Semantic status colors (success, warning, error, info, star) are available as Tailwind utilities"
    - "No dark mode section exists or is active"
    - "Legacy pencil-new.pen file does not exist in project root"
  artifacts:
    - path: "src/MicroCommerce.Web/src/app/globals.css"
      provides: "Full CSS variable theme with design-system.pen colors in oklch"
      contains: "--primary: oklch"
    - path: "src/MicroCommerce.Web/src/app/layout.tsx"
      provides: "DM Sans font via next/font/google"
      contains: "DM_Sans"
  key_links:
    - from: "src/MicroCommerce.Web/src/app/layout.tsx"
      to: "src/MicroCommerce.Web/src/app/globals.css"
      via: "CSS variable --font-dm-sans consumed by @theme inline --font-sans"
      pattern: "--font-dm-sans"
---

<objective>
Replace the default shadcn grayscale theme and Geist Sans font with the design-system.pen color palette (blue primary) and DM Sans font. This is the foundation for all subsequent screen plans.

Purpose: Every screen rewrite depends on correct CSS variables and font being loaded. Doing this first prevents rework.
Output: Updated globals.css with full oklch color palette + semantic status variables; updated layout.tsx with DM Sans font.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-CONTEXT.md
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-RESEARCH.md
@src/MicroCommerce.Web/src/app/globals.css
@src/MicroCommerce.Web/src/app/layout.tsx

<interfaces>
<!-- Current globals.css uses --font-geist-sans and --font-geist-mono in @theme inline -->
<!-- Current layout.tsx imports Geist and Geist_Mono from next/font/google -->
<!-- Both must be replaced entirely -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite globals.css with design color palette and semantic status variables</name>
  <files>src/MicroCommerce.Web/src/app/globals.css</files>
  <action>
Rewrite globals.css completely following the RESEARCH.md Example 1 pattern:

1. Keep the existing imports (`@import "tailwindcss"`, `@import "tw-animate-css"`) and `@custom-variant dark` line.

2. Replace the `@theme inline` block:
   - Change `--font-sans: var(--font-geist-sans)` to `--font-sans: var(--font-dm-sans)`
   - Remove `--font-mono: var(--font-geist-mono)` (not needed by design)
   - Add `--color-destructive-foreground: var(--destructive-foreground)` (missing from current)
   - Add all semantic status color mappings:
     - `--color-success: var(--success)`
     - `--color-success-bg: var(--success-bg)`
     - `--color-success-foreground: var(--success-foreground)`
     - `--color-warning: var(--warning)` / `--color-warning-bg` / `--color-warning-foreground`
     - `--color-error: var(--error)` / `--color-error-bg` / `--color-error-foreground`
     - `--color-info: var(--info)` / `--color-info-bg` / `--color-info-foreground`
     - `--color-star: var(--star)`

3. Replace the `:root` block with design-system.pen colors (hex -> oklch). Use the exact oklch values from RESEARCH.md "Example 1: globals.css Complete Theme Rewrite" section. Key values:
   - `--primary: oklch(0.546 0.245 262.881)` (#2563EB blue)
   - `--primary-foreground: oklch(1 0 0)` (#FFFFFF)
   - `--foreground: oklch(0.156 0 0)` (#1A1A1A)
   - `--border: oklch(0.928 0.006 264.531)` (#E5E7EB)
   - `--card: oklch(0.985 0.002 247.839)` (#F9FAFB)
   - `--accent: oklch(0.97 0.014 254.604)` (#EFF6FF blue tint)
   - `--accent-foreground: oklch(0.546 0.245 262.881)` (#2563EB)
   - `--ring: oklch(0.546 0.245 262.881)` (matches primary)
   - All semantic status variables (success, warning, error, info, star) per RESEARCH.md
   - All sidebar variables (light defaults for storefront)

4. Remove the `.dark` section entirely (per user decision: no dark mode, light mode only).

5. Keep the `@layer base` block unchanged.

PITFALL AVOIDANCE: For every new CSS variable added to `:root`, ensure the corresponding `--color-{name}: var(--{name})` exists in `@theme inline`. Missing this means Tailwind utilities like `bg-success` won't work.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && grep -c "oklch" src/app/globals.css | xargs test 30 -le && echo "PASS: 30+ oklch values" && grep -q "font-dm-sans" src/app/globals.css && echo "PASS: DM Sans reference" && grep -q "\-\-star:" src/app/globals.css && echo "PASS: Star variable" && grep -q "\-\-success:" src/app/globals.css && echo "PASS: Success variable" && ! grep -q "\.dark" src/app/globals.css && echo "PASS: No dark mode"</automated>
  </verify>
  <done>globals.css contains full design-system.pen color palette in oklch, semantic status variables, DM Sans font reference, no dark mode section, and all @theme inline mappings for new variables</done>
</task>

<task type="auto">
  <name>Task 2: Swap Geist Sans for DM Sans in root layout</name>
  <files>src/MicroCommerce.Web/src/app/layout.tsx</files>
  <action>
Replace font configuration in layout.tsx:

1. Replace import: `import { Geist, Geist_Mono } from "next/font/google"` with `import { DM_Sans } from "next/font/google"`

2. Replace font instantiation:
   - Remove `geistSans` and `geistMono` constants
   - Add:
     ```
     const dmSans = DM_Sans({
       subsets: ["latin"],
       weight: ["400", "500", "600"],
       variable: "--font-dm-sans",
       display: "swap",
     });
     ```

3. Update the html/body className:
   - From: `${geistSans.variable} ${geistMono.variable} antialiased`
   - To: `${dmSans.variable} antialiased`
   - Move className from `<body>` to `<html>` tag for cleaner next/font integration

4. Keep all other imports (Metadata, SessionProvider, globals.css) unchanged.
5. Keep metadata object unchanged.
6. Keep the SessionProvider wrapper unchanged.

PITFALL AVOIDANCE: The CSS variable name `--font-dm-sans` must match exactly what globals.css @theme inline references (`--font-sans: var(--font-dm-sans)`).
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && grep -q "DM_Sans" src/app/layout.tsx && echo "PASS: DM_Sans import" && grep -q "font-dm-sans" src/app/layout.tsx && echo "PASS: CSS variable" && ! grep -q "geist" src/app/layout.tsx && echo "PASS: No Geist references" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Root layout loads DM Sans with weights 400/500/600 via next/font/google, CSS variable --font-dm-sans is set, no Geist references remain, lint passes</done>
</task>

<task type="auto">
  <name>Task 3: Verify legacy pencil-new.pen is absent</name>
  <files>pencil-new.pen</files>
  <action>
Confirm the old monolithic pencil-new.pen file does not exist in the project root. Per locked user decision: "Old monolithic pencil-new.pen is deleted — split files replace it entirely."

1. Check that `pencil-new.pen` does NOT exist at the project root.
2. If it still exists for any reason, delete it: `rm pencil-new.pen`
3. Verify the split design files exist in `design/` (11 .pen files).

This is a guard task — the file was already removed when the design/ split files were created. This task ensures no stale copy lingers.
  </action>
  <verify>
    <automated>test ! -f pencil-new.pen && echo "PASS: pencil-new.pen absent" && ls design/*.pen | wc -l | xargs test 11 -eq && echo "PASS: 11 design files present"</automated>
  </verify>
  <done>pencil-new.pen does not exist in project root. All 11 split design/*.pen files are present.</done>
</task>

</tasks>

<verification>
- `grep -r "geist" src/MicroCommerce.Web/src/app/` returns no results (complete removal)
- `bg-primary` resolves to blue, not grayscale (visual check or CSS variable inspection)
- `bg-success`, `bg-warning`, `bg-error`, `bg-info`, `text-star` are valid Tailwind utilities
- `npm run lint` passes with no errors
</verification>

<success_criteria>
1. globals.css has 30+ oklch color values from design-system.pen
2. No Geist font references remain anywhere in src/app/
3. DM Sans font loads on all pages
4. Semantic status Tailwind utilities (bg-success, text-error, etc.) are functional
5. No dark mode CSS section exists
6. Biome lint passes
7. pencil-new.pen does not exist in project root (legacy file removed)
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-01-SUMMARY.md`
</output>
