---
phase: 25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn
plan: 05
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - src/MicroCommerce.Web/src/components/storefront/cart-page.tsx
  - src/MicroCommerce.Web/src/components/storefront/cart-item-row.tsx
  - src/MicroCommerce.Web/src/components/storefront/cart-summary.tsx
autonomous: true
requirements:
  - UI-CART
must_haves:
  truths:
    - "Cart page shows a table-like layout of items with image, name, quantity stepper, price, and remove button"
    - "Cart summary shows subtotal, shipping, tax, and total"
    - "Proceed to checkout button is prominent"
    - "Empty cart shows a friendly empty state"
  artifacts:
    - path: "src/MicroCommerce.Web/src/components/storefront/cart-page.tsx"
      provides: "Restyled cart page layout"
    - path: "src/MicroCommerce.Web/src/components/storefront/cart-item-row.tsx"
      provides: "Restyled cart item row with quantity stepper"
    - path: "src/MicroCommerce.Web/src/components/storefront/cart-summary.tsx"
      provides: "Restyled order summary sidebar"
  key_links:
    - from: "src/MicroCommerce.Web/src/components/storefront/cart-page.tsx"
      to: "src/MicroCommerce.Web/src/components/storefront/cart-item-row.tsx"
      via: "renders CartItemRow for each cart item"
      pattern: "CartItemRow"
    - from: "src/MicroCommerce.Web/src/components/storefront/cart-page.tsx"
      to: "src/MicroCommerce.Web/src/components/storefront/cart-summary.tsx"
      via: "renders CartSummary sidebar"
      pattern: "CartSummary"
---

<objective>
Restyle the shopping cart page with item table, quantity controls, order summary sidebar, and checkout CTA matching design/storefront-cart.pen.

Purpose: Cart is the pre-checkout funnel. Clear item display and summary drive checkout conversion.
Output: Restyled cart-page, cart-item-row, cart-summary components.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-CONTEXT.md
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-RESEARCH.md
@design/storefront-cart.pen
@design/design-system.pen
@src/MicroCommerce.Web/src/components/storefront/cart-page.tsx
@src/MicroCommerce.Web/src/components/storefront/cart-item-row.tsx
@src/MicroCommerce.Web/src/components/storefront/cart-summary.tsx
@src/MicroCommerce.Web/src/app/(storefront)/cart/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle cart-item-row with product image and quantity stepper</name>
  <files>
    src/MicroCommerce.Web/src/components/storefront/cart-item-row.tsx
  </files>
  <action>
Read design/storefront-cart.pen cart item row layout. Restyle cart-item-row.tsx:

1. Row layout: product image (small thumbnail) | product name + variant info | quantity stepper | unit price | line total | remove button
2. Use the QuantityStepper component (created in Plan 04) for quantity control. Import from `@/components/storefront/quantity-stepper`.
   - If QuantityStepper is not yet available (plans run in parallel), implement inline minus/value/plus with same styling pattern: `inline-flex items-center rounded-md border border-border`, minus/plus buttons, centered value
3. Product image: small square thumbnail (64x64 or 80x80) with rounded corners
4. Product name: `font-medium text-foreground`, variant details in `text-sm text-muted-foreground`
5. Price columns: `font-medium`, line total = unit price * quantity
6. Remove button: ghost variant icon button with Trash2 icon, `text-muted-foreground hover:text-destructive`
7. Row separator: `border-b border-border`
8. On mobile: stack layout (image + name on one line, quantity + price on next)

CRITICAL: Keep existing props (cart item data, onQuantityChange, onRemove callbacks) unchanged. Only rewrite JSX and Tailwind classes.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && grep -q "Trash\|trash" src/components/storefront/cart-item-row.tsx && echo "PASS: Remove button with trash icon" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Cart item row restyled with thumbnail, name, quantity controls, prices, and remove button matching design.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle cart-page layout and cart-summary sidebar</name>
  <files>
    src/MicroCommerce.Web/src/components/storefront/cart-page.tsx,
    src/MicroCommerce.Web/src/components/storefront/cart-summary.tsx
  </files>
  <action>
1. **Restyle cart-summary.tsx**: Read design/storefront-cart.pen summary section.
   - Sticky sidebar card: use shadcn Card component
   - Line items: Subtotal, Shipping (calculated or "Free"), Tax, horizontal rule, Total (bold, larger)
   - Each line: flex justify-between with label + value
   - Total uses `text-lg font-semibold`
   - "Proceed to Checkout" button: full-width, primary variant, prominent
   - Optional promo code input field below button
   - Keep existing price calculation logic unchanged

2. **Restyle cart-page.tsx**: Read design/storefront-cart.pen full page layout.
   - Page heading: "Shopping Cart" with item count in parentheses
   - Two-column layout: left = cart items list (wider, ~2/3), right = cart summary sidebar (sticky)
   - Cart items list header row: "Product", "Quantity", "Price", "Total" column headers
   - Map CartItemRow components for each item
   - Empty state: centered illustration/icon, "Your cart is empty" heading, "Continue Shopping" link button
   - "Continue Shopping" link at top or below items
   - Container: `max-w-7xl mx-auto px-4 py-8`
   - Responsive: on mobile, summary moves below items (single column)
   - Keep all existing cart hooks (useCart) and data fetching unchanged

CRITICAL: Do not modify any cart state management, useCart hook calls, or API interactions.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && grep -q "Shopping Cart\|shopping-cart\|ShoppingCart" src/components/storefront/cart-page.tsx && echo "PASS: Cart heading" && grep -q "Checkout\|checkout" src/components/storefront/cart-summary.tsx && echo "PASS: Checkout CTA" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Cart page restyled with two-column layout (items + summary sidebar), column headers, empty state, and prominent checkout button.</done>
</task>

</tasks>

<verification>
- Cart page shows two-column layout (items list + summary sidebar)
- Each item row has thumbnail, name, quantity controls, price, remove button
- Summary shows subtotal/shipping/tax/total with checkout button
- Empty cart shows friendly empty state
- `npm run lint` passes
</verification>

<success_criteria>
1. Cart items display in table-like layout with thumbnails and quantity controls
2. Cart summary is a sticky sidebar card with line-item pricing
3. "Proceed to Checkout" button is full-width and primary color
4. Empty cart state is user-friendly
5. Responsive layout (single column on mobile)
6. Biome lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-05-SUMMARY.md`
</output>
