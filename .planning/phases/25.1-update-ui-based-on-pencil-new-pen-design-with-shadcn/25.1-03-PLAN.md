---
phase: 25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn
plan: 03
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - src/MicroCommerce.Web/src/components/storefront/product-filters.tsx
  - src/MicroCommerce.Web/src/components/storefront/product-grid.tsx
  - src/MicroCommerce.Web/src/components/storefront/search-bar.tsx
  - src/MicroCommerce.Web/src/components/ui/checkbox.tsx
  - src/MicroCommerce.Web/src/components/ui/slider.tsx
  - src/MicroCommerce.Web/src/components/ui/pagination.tsx
autonomous: true
requirements:
  - UI-CATALOG
must_haves:
  truths:
    - "Product catalog shows a left sidebar with category checkboxes, price range slider, and rating filter"
    - "Product grid displays in a responsive grid (4 cols desktop, 2 tablet, 1 mobile)"
    - "Pagination controls appear below the product grid"
    - "Search bar filters products"
  artifacts:
    - path: "src/MicroCommerce.Web/src/components/storefront/product-filters.tsx"
      provides: "Left sidebar filter panel with checkboxes, slider, rating"
    - path: "src/MicroCommerce.Web/src/components/storefront/product-grid.tsx"
      provides: "Responsive product grid with pagination"
    - path: "src/MicroCommerce.Web/src/components/ui/checkbox.tsx"
      provides: "shadcn checkbox component"
    - path: "src/MicroCommerce.Web/src/components/ui/slider.tsx"
      provides: "shadcn slider component"
    - path: "src/MicroCommerce.Web/src/components/ui/pagination.tsx"
      provides: "shadcn pagination component"
  key_links:
    - from: "src/MicroCommerce.Web/src/components/storefront/product-filters.tsx"
      to: "src/MicroCommerce.Web/src/components/ui/checkbox.tsx"
      via: "import Checkbox for category filter"
      pattern: "Checkbox"
    - from: "src/MicroCommerce.Web/src/components/storefront/product-filters.tsx"
      to: "src/MicroCommerce.Web/src/components/ui/slider.tsx"
      via: "import Slider for price range"
      pattern: "Slider"
---

<objective>
Restyle the product catalog with left sidebar filters (category checkboxes, price range slider, rating filter) and paginated product grid matching design/storefront-catalog.pen.

Purpose: Catalog is the main product browsing experience. Filters and pagination are critical for usability.
Output: Restyled product-filters, product-grid, search-bar + 3 new shadcn components installed.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-CONTEXT.md
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-RESEARCH.md
@design/storefront-catalog.pen
@design/design-system.pen
@src/MicroCommerce.Web/src/components/storefront/product-filters.tsx
@src/MicroCommerce.Web/src/components/storefront/product-grid.tsx
@src/MicroCommerce.Web/src/components/storefront/search-bar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn checkbox, slider, pagination and restyle product-filters</name>
  <files>
    src/MicroCommerce.Web/src/components/ui/checkbox.tsx,
    src/MicroCommerce.Web/src/components/ui/slider.tsx,
    src/MicroCommerce.Web/src/components/ui/pagination.tsx,
    src/MicroCommerce.Web/src/components/storefront/product-filters.tsx
  </files>
  <action>
1. **Install shadcn components** (run from src/MicroCommerce.Web):
   ```
   npx shadcn@latest add checkbox slider pagination
   ```
   This creates checkbox.tsx, slider.tsx, pagination.tsx in src/components/ui/

2. **Restyle product-filters.tsx**: Read design/storefront-catalog.pen filter sidebar section. Rewrite JSX:
   - Vertical sidebar layout with sections: "Categories", "Price Range", "Rating"
   - Categories section: list of shadcn Checkbox items with labels (one per category)
   - Price Range section: shadcn Slider (dual thumb for min/max) with displayed values
   - Rating section: clickable star rows (5 stars, 4+ stars, 3+ stars, etc.)
   - "Clear Filters" button at bottom
   - Section headings: `text-sm font-semibold text-foreground`
   - Dividers between sections: `border-b border-border`
   - On mobile: filters collapse into a Sheet/drawer (use existing shadcn Sheet)
   - Keep existing filter state management, onChange handlers, and category data unchanged

CRITICAL: Keep all existing filter state hooks and callback props. Only change the rendering JSX.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && test -f src/components/ui/checkbox.tsx && test -f src/components/ui/slider.tsx && test -f src/components/ui/pagination.tsx && echo "PASS: shadcn components installed" && grep -q "Checkbox" src/components/storefront/product-filters.tsx && echo "PASS: Checkbox used in filters" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>shadcn checkbox, slider, pagination installed. product-filters restyled with category checkboxes, price range slider, rating filter, and mobile-responsive Sheet fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle product-grid and search-bar with pagination</name>
  <files>
    src/MicroCommerce.Web/src/components/storefront/product-grid.tsx,
    src/MicroCommerce.Web/src/components/storefront/search-bar.tsx
  </files>
  <action>
1. **Restyle product-grid.tsx**: Read design/storefront-catalog.pen grid layout. Rewrite JSX:
   - Responsive grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6`
   - Results count text above grid: "Showing X of Y products"
   - Sort dropdown (use shadcn Select) for "Price: Low to High", "Price: High to Low", "Newest", "Rating"
   - Pagination component below grid using the newly installed shadcn Pagination
   - Empty state when no products: centered text with illustration placeholder
   - Keep existing product data mapping, loading states, and skeleton placeholders
   - The grid layout should work alongside the sidebar filters (two-column layout: sidebar + grid)

2. **Restyle search-bar.tsx**: Read design/storefront-catalog.pen search section.
   - Input with Search icon on left inside a rounded border container
   - Use shadcn Input as base, wrap with search icon
   - `placeholder="Search products..."` with `rounded-lg` border
   - Keep existing search state, debounce logic, and onChange handlers

CRITICAL: Preserve all data fetching, pagination state, sort state, and search debounce logic.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && grep -q "grid-cols" src/components/storefront/product-grid.tsx && echo "PASS: Grid layout" && grep -q "Pagination" src/components/storefront/product-grid.tsx && echo "PASS: Pagination used" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Product grid restyled with responsive columns, sort dropdown, and pagination. Search bar restyled with icon and rounded design. All existing logic preserved.</done>
</task>

</tasks>

<verification>
- Product catalog page shows sidebar filters + product grid in two-column layout
- Checkbox filters, price slider, and rating filter are interactive
- Pagination renders below grid
- Search bar has search icon
- `npm run lint` passes
</verification>

<success_criteria>
1. shadcn checkbox, slider, pagination components are installed
2. Product filters use shadcn Checkbox for categories and Slider for price range
3. Product grid is responsive (4/3/2/1 columns)
4. Pagination appears below grid
5. Search bar has icon and rounded styling
6. Biome lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-03-SUMMARY.md`
</output>
