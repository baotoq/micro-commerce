---
phase: 25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn
plan: 04
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - src/MicroCommerce.Web/src/components/storefront/product-detail.tsx
  - src/MicroCommerce.Web/src/components/storefront/related-products.tsx
  - src/MicroCommerce.Web/src/components/storefront/quantity-stepper.tsx
  - src/MicroCommerce.Web/src/components/reviews/review-list.tsx
  - src/MicroCommerce.Web/src/components/reviews/review-form-dialog.tsx
  - src/MicroCommerce.Web/src/components/reviews/star-rating-display.tsx
  - src/MicroCommerce.Web/src/components/ui/tabs.tsx
  - src/MicroCommerce.Web/src/components/ui/breadcrumb.tsx
  - src/MicroCommerce.Web/src/app/(storefront)/products/[id]/page.tsx
autonomous: true
requirements:
  - UI-PRODUCT-DETAIL
must_haves:
  truths:
    - "Product detail page shows image gallery with thumbnails"
    - "Tab section switches between Description and Reviews & Ratings"
    - "QuantityStepper allows incrementing/decrementing quantity before add-to-cart"
    - "Reviews display with star ratings using amber star color"
    - "Breadcrumb navigation shows path back to catalog"
    - "Related products section shows at bottom"
  artifacts:
    - path: "src/MicroCommerce.Web/src/components/storefront/quantity-stepper.tsx"
      provides: "New QuantityStepper component (minus/value/plus)"
    - path: "src/MicroCommerce.Web/src/components/ui/tabs.tsx"
      provides: "shadcn tabs component"
    - path: "src/MicroCommerce.Web/src/components/ui/breadcrumb.tsx"
      provides: "shadcn breadcrumb component"
    - path: "src/MicroCommerce.Web/src/components/storefront/product-detail.tsx"
      provides: "Restyled product detail with gallery, tabs, quantity"
  key_links:
    - from: "src/MicroCommerce.Web/src/components/storefront/product-detail.tsx"
      to: "src/MicroCommerce.Web/src/components/ui/tabs.tsx"
      via: "Tabs/TabsList/TabsTrigger/TabsContent for Description/Reviews"
      pattern: "Tabs"
    - from: "src/MicroCommerce.Web/src/components/storefront/product-detail.tsx"
      to: "src/MicroCommerce.Web/src/components/storefront/quantity-stepper.tsx"
      via: "import QuantityStepper for quantity selection"
      pattern: "QuantityStepper"
---

<objective>
Restyle the product detail page with image gallery, tabbed content (Description / Reviews), QuantityStepper, breadcrumb navigation, and related products matching design/storefront-product-detail.pen.

Purpose: Product detail is the key conversion page. Image gallery, tabs, and review display drive purchase decisions.
Output: Restyled product-detail, new QuantityStepper component, installed tabs + breadcrumb, restyled review components.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-CONTEXT.md
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-RESEARCH.md
@design/storefront-product-detail.pen
@design/design-system.pen
@src/MicroCommerce.Web/src/components/storefront/product-detail.tsx
@src/MicroCommerce.Web/src/components/storefront/related-products.tsx
@src/MicroCommerce.Web/src/components/reviews/review-list.tsx
@src/MicroCommerce.Web/src/components/reviews/review-form-dialog.tsx
@src/MicroCommerce.Web/src/components/reviews/star-rating-display.tsx
@src/MicroCommerce.Web/src/app/(storefront)/products/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install tabs + breadcrumb, create QuantityStepper, restyle product-detail</name>
  <files>
    src/MicroCommerce.Web/src/components/ui/tabs.tsx,
    src/MicroCommerce.Web/src/components/ui/breadcrumb.tsx,
    src/MicroCommerce.Web/src/components/storefront/quantity-stepper.tsx,
    src/MicroCommerce.Web/src/components/storefront/product-detail.tsx,
    src/MicroCommerce.Web/src/app/(storefront)/products/[id]/page.tsx
  </files>
  <action>
1. **Install shadcn components** (run from src/MicroCommerce.Web):
   ```
   npx shadcn@latest add tabs breadcrumb
   ```

2. **Create QuantityStepper** (`src/components/storefront/quantity-stepper.tsx`):
   - Use RESEARCH.md "Example 4: QuantityStepper Custom Component" as template exactly
   - "use client" directive (has interactive state)
   - Props: `value: number`, `onIncrement: () => void`, `onDecrement: () => void`, `min?: number`, `max?: number`
   - Layout: inline-flex with Minus button | value display | Plus button
   - Border container with `rounded-md border border-border`
   - Disabled states when at min/max
   - ARIA labels on buttons

3. **Restyle product-detail.tsx**: Read design/storefront-product-detail.pen. Rewrite JSX:
   - Two-column layout: left = image gallery, right = product info
   - Image gallery: main image + thumbnail row below (clickable thumbnails switch main image)
   - Product info column: name, price (with original/sale), rating summary, description snippet, QuantityStepper, Add to Cart button, wishlist button
   - Below the two columns: Tab section using shadcn Tabs
     - Tab 1: "Description" (full product description)
     - Tab 2: "Reviews & Ratings" (review list + submit form)
   - Breadcrumb at top: Home > Products > {Product Name}
   - Keep all existing data fetching, hooks (useProduct, useCart, useWishlist, etc.) unchanged

4. **Update products/[id]/page.tsx**: Add breadcrumb to page if not already in product-detail component. Ensure layout wrapper uses `max-w-7xl mx-auto` pattern. Keep data fetching unchanged.

CRITICAL: Preserve all existing hooks, API calls, and data flow. Only rewrite JSX/CSS.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && test -f src/components/ui/tabs.tsx && test -f src/components/ui/breadcrumb.tsx && test -f src/components/storefront/quantity-stepper.tsx && echo "PASS: Components created" && grep -q "Tabs" src/components/storefront/product-detail.tsx && echo "PASS: Tabs used" && grep -q "QuantityStepper" src/components/storefront/product-detail.tsx && echo "PASS: QuantityStepper used" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>shadcn tabs and breadcrumb installed. QuantityStepper created. Product detail restyled with image gallery, two-column layout, tabbed content, breadcrumb.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle review components and related-products</name>
  <files>
    src/MicroCommerce.Web/src/components/reviews/star-rating-display.tsx,
    src/MicroCommerce.Web/src/components/reviews/review-list.tsx,
    src/MicroCommerce.Web/src/components/reviews/review-form-dialog.tsx,
    src/MicroCommerce.Web/src/components/storefront/related-products.tsx
  </files>
  <action>
1. **Restyle star-rating-display.tsx**: Update star color from any hardcoded color to `text-star` (uses --star CSS variable = amber #FBBF24). Keep existing star rendering logic (filled vs empty stars).

2. **Restyle review-list.tsx**: Read design/storefront-product-detail.pen review section.
   - Each review: avatar, reviewer name, date, star rating, review text, verified purchase badge
   - Use shadcn Avatar for reviewer avatar
   - Use shadcn Badge with "success" styling for verified purchase indicator
   - Clean card-like layout per review with border separator
   - Rating summary at top: average rating, star breakdown bar chart
   - Keep existing data mapping and pagination unchanged

3. **Restyle review-form-dialog.tsx**: Update dialog styling to match design:
   - Use shadcn Dialog (already installed)
   - Star rating input with interactive hover state
   - Textarea for review text
   - Submit button using primary variant
   - Keep existing form submission logic unchanged

4. **Restyle related-products.tsx**: Read design/storefront-product-detail.pen related section.
   - Horizontal scroll or grid row of 4 ProductCard components
   - Section heading: "Related Products" or "You Might Also Like"
   - Use the ProductCard restyled in Plan 02
   - Keep existing data fetching unchanged

CRITICAL: star-rating-display must use `text-star` Tailwind class (not hardcoded hex).
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && grep -q "text-star" src/components/reviews/star-rating-display.tsx && echo "PASS: Star color uses theme variable" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Star rating uses --star theme variable. Review list and form restyled with avatars, badges, clean layout. Related products shows horizontal card row.</done>
</task>

</tasks>

<verification>
- Product detail page shows two-column layout with image gallery and product info
- Tabs switch between Description and Reviews
- QuantityStepper increments/decrements quantity
- Star ratings are amber colored (--star variable)
- Breadcrumb shows navigation path
- Related products section visible at bottom
- `npm run lint` passes
</verification>

<success_criteria>
1. shadcn tabs and breadcrumb components installed
2. QuantityStepper component exists with min/max/ARIA support
3. Product detail has image gallery, tabbed content, breadcrumb
4. Star ratings use `text-star` (amber from theme)
5. Review list shows avatars, verified badges, star ratings
6. Related products displays as card row
7. Biome lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-04-SUMMARY.md`
</output>
