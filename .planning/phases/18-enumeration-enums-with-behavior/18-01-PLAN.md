---
phase: 18-enumeration-enums-with-behavior
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BuildingBlocks/BuildingBlocks.Common/BuildingBlocks.Common.csproj
  - src/BuildingBlocks/BuildingBlocks.Common/Converters/SmartEnumStringConverter.cs
  - src/MicroCommerce.ApiService/MicroCommerce.ApiService.csproj
  - src/MicroCommerce.ApiService/Program.cs
  - src/MicroCommerce.ApiService/Common/Persistence/BaseDbContext.cs
  - src/MicroCommerce.ApiService/Features/Ordering/Domain/ValueObjects/OrderStatus.cs
  - src/MicroCommerce.ApiService/Features/Catalog/Domain/ValueObjects/ProductStatus.cs
autonomous: true
requirements:
  - PRIM-02

must_haves:
  truths:
    - "OrderStatus and ProductStatus are Ardalis.SmartEnum types with CanTransitionTo() and TransitionTo() encapsulating state machine rules"
    - "EF Core stores SmartEnum status properties as strings matching the existing database schema (no migration needed)"
    - "JSON serialization produces plain string values (e.g. 'Submitted') not object format"
    - "Existing code that compares OrderStatus.X == entity.Status continues to work via SmartEnum singleton equality"
  artifacts:
    - path: "src/BuildingBlocks/BuildingBlocks.Common/Converters/SmartEnumStringConverter.cs"
      provides: "Generic EF Core ValueConverter storing SmartEnum by Name (string)"
      contains: "SmartEnumStringConverter"
    - path: "src/MicroCommerce.ApiService/Features/Ordering/Domain/ValueObjects/OrderStatus.cs"
      provides: "OrderStatus SmartEnum with 8 states and transition rules"
      contains: "SmartEnum<OrderStatus>"
    - path: "src/MicroCommerce.ApiService/Features/Catalog/Domain/ValueObjects/ProductStatus.cs"
      provides: "ProductStatus SmartEnum with 3 states and transition rules"
      contains: "SmartEnum<ProductStatus>"
  key_links:
    - from: "src/MicroCommerce.ApiService/Common/Persistence/BaseDbContext.cs"
      to: "SmartEnumStringConverter"
      via: "ConfigureConventions Properties<T>().HaveConversion<>()"
      pattern: "Properties<OrderStatus>"
    - from: "src/MicroCommerce.ApiService/Program.cs"
      to: "SmartEnumNameConverter"
      via: "ConfigureHttpJsonOptions"
      pattern: "SmartEnumNameConverter<OrderStatus"
---

<objective>
Install Ardalis.SmartEnum packages, create generic EF Core string converter, replace plain OrderStatus and ProductStatus enums with SmartEnum abstract classes containing state transition rules, and register EF Core + JSON converters globally.

Purpose: Establish SmartEnum infrastructure (PRIM-02) — generic converters and the two SmartEnum types — so that Plan 02 can migrate entity methods and query handlers.
Output: Two SmartEnum types with transition rules, generic EF Core converter, JSON serialization working as plain strings.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-enumeration-enums-with-behavior/18-CONTEXT.md
@.planning/phases/18-enumeration-enums-with-behavior/18-RESEARCH.md
@src/BuildingBlocks/BuildingBlocks.Common/BuildingBlocks.Common.csproj
@src/MicroCommerce.ApiService/MicroCommerce.ApiService.csproj
@src/MicroCommerce.ApiService/Common/Persistence/BaseDbContext.cs
@src/MicroCommerce.ApiService/Program.cs
@src/MicroCommerce.ApiService/Features/Ordering/Domain/ValueObjects/OrderStatus.cs
@src/MicroCommerce.ApiService/Features/Catalog/Domain/ValueObjects/ProductStatus.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and create SmartEnum types with transition rules</name>
  <files>
    src/BuildingBlocks/BuildingBlocks.Common/BuildingBlocks.Common.csproj
    src/MicroCommerce.ApiService/MicroCommerce.ApiService.csproj
    src/MicroCommerce.ApiService/Features/Ordering/Domain/ValueObjects/OrderStatus.cs
    src/MicroCommerce.ApiService/Features/Catalog/Domain/ValueObjects/ProductStatus.cs
  </files>
  <action>
**Step 1: Install NuGet packages**
- Add `Ardalis.SmartEnum` version 8.2.0 to `BuildingBlocks.Common.csproj`
- Add `Ardalis.SmartEnum.SystemTextJson` version 8.1.0 to `MicroCommerce.ApiService.csproj`
- Do NOT add `Ardalis.SmartEnum.EFCore` — we build a custom string converter (the EFCore package stores by int Value, not string Name, which would break the existing schema)

**Step 2: Replace OrderStatus enum**
Replace the entire `OrderStatus.cs` file. The new type is an abstract class inheriting `SmartEnum<OrderStatus>` with 8 sealed inner classes. Each inner class overrides `CanTransitionTo()`.

Preserve the existing enum member names exactly (Submitted, StockReserved, Paid, Confirmed, Shipped, Delivered, Failed, Cancelled) — these are the strings stored in the database.

Integer backing values (Claude's discretion): use 0-7 matching the original enum ordinals: Submitted=0, StockReserved=1, Paid=2, Confirmed=3, Shipped=4, Delivered=5, Failed=6, Cancelled=7.

State transition rules (locked decisions from CONTEXT.md):
- **Submitted** → StockReserved, Paid, Failed, Cancelled
- **StockReserved** → Paid, Failed, Cancelled
- **Paid** → Confirmed, Cancelled
- **Confirmed** → Shipped
- **Shipped** → Delivered
- **Delivered** → (terminal, no outbound)
- **Failed** → (terminal, no outbound)
- **Cancelled** → (terminal, no outbound)

Add two public methods on the base:
- `abstract bool CanTransitionTo(OrderStatus next)` — each inner class overrides
- `void TransitionTo(OrderStatus next)` — validates via CanTransitionTo(), throws `InvalidOperationException` with message: `"Cannot transition from '{Name}' to '{next.Name}'. Valid transitions from '{Name}': {comma-separated valid names}."` per CONTEXT.md decision

Keep the class open (not sealed) per CONTEXT.md. Use file-scoped namespace.

**Step 3: Replace ProductStatus enum**
Replace the entire `ProductStatus.cs` file. Same abstract SmartEnum pattern.

Members: Draft=0, Published=1, Archived=2.

Transition rules:
- **Draft** → Published, Archived
- **Published** → Draft, Archived
- **Archived** → (terminal, no outbound)

Same `CanTransitionTo()` + `TransitionTo()` pattern. Keep class open. Use file-scoped namespace.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/micro-commerce && dotnet build src/BuildingBlocks/BuildingBlocks.Common/BuildingBlocks.Common.csproj --no-restore 2>&amp;1 | tail -5</automated>
    <manual>Verify SmartEnum types have correct transition rules</manual>
  </verify>
  <done>
    - Ardalis.SmartEnum 8.2.0 in BuildingBlocks.Common.csproj
    - Ardalis.SmartEnum.SystemTextJson 8.1.0 in MicroCommerce.ApiService.csproj
    - OrderStatus is an abstract SmartEnum with 8 states, CanTransitionTo(), and TransitionTo()
    - ProductStatus is an abstract SmartEnum with 3 states, CanTransitionTo(), and TransitionTo()
    - OrderStatus transition rules match CONTEXT.md exactly
    - ProductStatus transition rules match CONTEXT.md exactly
  </done>
</task>

<task type="auto">
  <name>Task 2: Create generic EF Core converter and register EF Core + JSON converters</name>
  <files>
    src/BuildingBlocks/BuildingBlocks.Common/Converters/SmartEnumStringConverter.cs
    src/MicroCommerce.ApiService/Common/Persistence/BaseDbContext.cs
    src/MicroCommerce.ApiService/Program.cs
  </files>
  <action>
**Step 1: Create SmartEnumStringConverter**
Create new file `src/BuildingBlocks/BuildingBlocks.Common/Converters/SmartEnumStringConverter.cs`.

This is a generic `ValueConverter<TEnum, string>` that converts SmartEnum to/from its Name property:
```
namespace MicroCommerce.BuildingBlocks.Common.Converters;

public class SmartEnumStringConverter<TEnum> : ValueConverter<TEnum, string>
    where TEnum : SmartEnum<TEnum, int>
{
    public SmartEnumStringConverter()
        : base(
            smartEnum => smartEnum.Name,
            name => SmartEnum<TEnum, int>.FromName(name))
    { }
}
```
This stores by Name (string) NOT by Value (int). This is critical because the existing database schema stores strings ("Submitted", "Paid", etc.).

BuildingBlocks.Common already has the `Ardalis.SmartEnum` package reference. Need to add `using Ardalis.SmartEnum;` and `using Microsoft.EntityFrameworkCore.Storage.ValueConversion;`. BuildingBlocks.Common does NOT have an EF Core package reference — add `Microsoft.EntityFrameworkCore` package reference (just the abstractions, not a provider) to BuildingBlocks.Common.csproj so `ValueConverter` is available. Use same major version as ApiService (10.0.0).

**Step 2: Register EF Core converters in BaseDbContext**
In `BaseDbContext.ConfigureConventions()`, add per-type SmartEnum converter registrations AFTER `RegisterAllInVogenEfCoreConverters()` and BEFORE the convention Add calls:
```
configurationBuilder.Properties<OrderStatus>()
    .HaveConversion<SmartEnumStringConverter<OrderStatus>>();
configurationBuilder.Properties<ProductStatus>()
    .HaveConversion<SmartEnumStringConverter<ProductStatus>>();
```
Add required using statements for the SmartEnum types and the converter.

**Step 3: Register JSON converters in Program.cs**
Add `ConfigureHttpJsonOptions` in Program.cs after the `builder.Services.AddProblemDetails();` line and before `builder.Services.AddOpenApi();`:
```
builder.Services.ConfigureHttpJsonOptions(options =>
{
    options.SerializerOptions.Converters.Add(
        new SmartEnumNameConverter<OrderStatus, int>());
    options.SerializerOptions.Converters.Add(
        new SmartEnumNameConverter<ProductStatus, int>());
});
```
This uses `SmartEnumNameConverter` from `Ardalis.SmartEnum.SystemTextJson` so SmartEnum serializes as plain string "Submitted" not as object `{"name":"Submitted","value":0}`.

Add required usings:
- `using Ardalis.SmartEnum.SystemTextJson;`
- `using MicroCommerce.ApiService.Features.Ordering.Domain.ValueObjects;`
- `using MicroCommerce.ApiService.Features.Catalog.Domain.ValueObjects;`
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/micro-commerce && dotnet build src/MicroCommerce.ApiService/MicroCommerce.ApiService.csproj 2>&amp;1 | tail -5</automated>
    <manual>Verify the full project builds cleanly with no warnings</manual>
  </verify>
  <done>
    - SmartEnumStringConverter.cs exists in BuildingBlocks.Common/Converters/ with generic ValueConverter<TEnum, string>
    - BaseDbContext.ConfigureConventions registers SmartEnumStringConverter for both OrderStatus and ProductStatus
    - Program.cs registers SmartEnumNameConverter for both types via ConfigureHttpJsonOptions
    - Full project builds with zero errors and zero warnings
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/MicroCommerce.ApiService/MicroCommerce.ApiService.csproj` completes with 0 errors, 0 warnings
2. OrderStatus.cs contains `SmartEnum<OrderStatus>` base class with 8 sealed inner classes
3. ProductStatus.cs contains `SmartEnum<ProductStatus>` base class with 3 sealed inner classes
4. BaseDbContext.ConfigureConventions includes `Properties<OrderStatus>().HaveConversion<SmartEnumStringConverter<OrderStatus>>()`
5. Program.cs includes `ConfigureHttpJsonOptions` with `SmartEnumNameConverter<OrderStatus, int>` and `SmartEnumNameConverter<ProductStatus, int>`
</verification>

<success_criteria>
- SmartEnum infrastructure (PRIM-02) is fully in place: packages, generic converter, two SmartEnum types, EF Core registration, JSON registration
- Both SmartEnum types encapsulate correct state transition rules per CONTEXT.md locked decisions
- Database schema compatibility preserved (string storage via SmartEnumStringConverter)
- JSON API compatibility preserved (plain string serialization via SmartEnumNameConverter)
- Project builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/18-enumeration-enums-with-behavior/18-01-SUMMARY.md`
</output>
