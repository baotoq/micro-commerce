---
phase: 03-catalog-storefront-seed-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - code/MicroCommerce.Web/src/app/(storefront)/layout.tsx
  - code/MicroCommerce.Web/src/app/(storefront)/page.tsx
  - code/MicroCommerce.Web/src/components/storefront/header.tsx
  - code/MicroCommerce.Web/src/components/storefront/footer.tsx
  - code/MicroCommerce.Web/src/components/storefront/hero-banner.tsx
  - code/MicroCommerce.Web/src/lib/api.ts
autonomous: true

must_haves:
  truths:
    - "Visiting / shows a storefront with header, hero banner, and footer"
    - "Header contains site name/logo, search bar placeholder, and cart icon placeholder"
    - "Storefront layout does not affect admin pages at /admin/*"
  artifacts:
    - path: "code/MicroCommerce.Web/src/app/(storefront)/layout.tsx"
      provides: "Storefront layout wrapper with header and footer"
      min_lines: 15
    - path: "code/MicroCommerce.Web/src/components/storefront/header.tsx"
      provides: "Site header with navigation, search placeholder, cart icon"
      min_lines: 30
    - path: "code/MicroCommerce.Web/src/components/storefront/hero-banner.tsx"
      provides: "Hero section for homepage"
      min_lines: 20
    - path: "code/MicroCommerce.Web/src/components/storefront/footer.tsx"
      provides: "Site footer"
      min_lines: 10
  key_links:
    - from: "src/app/(storefront)/layout.tsx"
      to: "src/components/storefront/header.tsx"
      via: "import and render"
      pattern: "import.*header"
    - from: "src/app/(storefront)/page.tsx"
      to: "src/components/storefront/hero-banner.tsx"
      via: "import and render"
      pattern: "import.*hero"
---

<objective>
Create the storefront layout shell and homepage hero section. Install required shadcn/ui components.

Purpose: Establishes the (storefront) route group with its own layout (header/footer) separate from admin. The hero banner creates the Apple Store-inspired landing experience. This is the foundation all other storefront pages render within.
Output: Working storefront layout at / with header (logo, search placeholder, cart icon), hero banner, and footer. Admin pages unaffected.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-catalog-storefront-seed-data/03-CONTEXT.md
@.planning/phases/03-catalog-storefront-seed-data/03-RESEARCH.md

@code/MicroCommerce.Web/src/app/layout.tsx
@code/MicroCommerce.Web/src/app/page.tsx
@code/MicroCommerce.Web/src/app/admin/layout.tsx
@code/MicroCommerce.Web/src/lib/api.ts
@code/MicroCommerce.Web/next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui components and create storefront layout</name>
  <files>
    code/MicroCommerce.Web/src/app/(storefront)/layout.tsx
    code/MicroCommerce.Web/src/app/(storefront)/page.tsx
    code/MicroCommerce.Web/src/components/storefront/header.tsx
    code/MicroCommerce.Web/src/components/storefront/footer.tsx
  </files>
  <action>
    First, install needed shadcn/ui components:
    ```bash
    cd code/MicroCommerce.Web && npx shadcn@latest add card separator aspect-ratio -y
    ```

    Then create the storefront layout and components:

    1. **Move the current `src/app/page.tsx`** content into `src/app/(storefront)/page.tsx` (this will be replaced with the homepage in a moment). The old `src/app/page.tsx` should be DELETED since the (storefront) route group captures the / route.

    2. **Create `src/app/(storefront)/layout.tsx`:**
       - Import and render Header at top and Footer at bottom
       - Main content area between them with `min-h-screen` flex layout
       - Clean, minimal wrapper -- no sidebar

    3. **Create `src/components/storefront/header.tsx`:**
       - Apple Store-inspired minimal header
       - Left: "MicroCommerce" text logo (link to /)
       - Center: Navigation links (just "Products" for now)
       - Right: Search icon (placeholder, non-functional yet), Cart icon with badge placeholder (non-functional until Cart phase)
       - Sticky header with backdrop blur on scroll
       - Use lucide-react icons (Search, ShoppingCart)
       - Include a mobile-responsive design (hamburger menu or simplified layout on small screens)

    4. **Create `src/components/storefront/footer.tsx`:**
       - Minimal footer with copyright, "Built with Next.js & .NET" tagline
       - Links: "Admin" (links to /admin), "GitHub" (placeholder #)
       - Muted colors, simple layout

    5. **Update `src/app/layout.tsx`** metadata: Change title to "MicroCommerce" and description to "Premium electronics store".

    Design aesthetic: Apple Store -- clean white/light backgrounds, generous whitespace, subtle borders, system font stack (Geist is already configured). Use Tailwind neutral/zinc palette.
  </action>
  <verify>
    `cd code/MicroCommerce.Web && npm run build` succeeds. Verify card, separator, aspect-ratio components exist in src/components/ui/. Verify (storefront) route group has layout.tsx and page.tsx. Verify old src/app/page.tsx is removed.
  </verify>
  <done>
    Storefront layout renders at / with header and footer. Admin pages at /admin/* are unaffected. shadcn/ui card, separator, and aspect-ratio components installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create hero banner component and homepage</name>
  <files>
    code/MicroCommerce.Web/src/components/storefront/hero-banner.tsx
    code/MicroCommerce.Web/src/app/(storefront)/page.tsx
  </files>
  <action>
    1. **Create `src/components/storefront/hero-banner.tsx`:**
       - Large hero section with gradient or clean white background
       - Headline: "The Latest in Tech" or similar premium-feeling copy
       - Subtitle: Brief tagline about browsing the collection
       - CTA button: "Browse Products" that scrolls to product grid section (or links to #products anchor)
       - Apple Store vibe: large typography, generous padding (py-24 or more), centered text
       - Optional: subtle gradient background or a decorative element
       - `use client` is fine if needed for scroll behavior, otherwise keep as server component

    2. **Update `src/app/(storefront)/page.tsx`:**
       - Render HeroBanner at top
       - Below hero, add a "Featured Products" section heading (products grid will be added in Plan 03)
       - Add a placeholder div with id="products" for the product grid section with a brief "Loading products..." skeleton or text
       - This page will be extended in Plan 03 to include the actual product grid

    The homepage should feel like a polished landing page even before the product grid is wired up.
  </action>
  <verify>
    `cd code/MicroCommerce.Web && npm run build` succeeds. Read the page.tsx to confirm hero banner is rendered and products section placeholder exists.
  </verify>
  <done>
    Homepage at / shows hero banner with headline, subtitle, and CTA. Below hero is a "Featured Products" section placeholder ready for the product grid.
  </done>
</task>

</tasks>

<verification>
- `npm run build` in MicroCommerce.Web succeeds
- / route shows storefront layout (header + hero + footer)
- /admin route still shows admin layout (not storefront)
- Header has logo, nav, search icon placeholder, cart icon placeholder
- Hero banner has headline, subtitle, and CTA button
</verification>

<success_criteria>
- (storefront) route group created with its own layout
- Header is sticky with Apple Store aesthetic
- Hero banner renders with premium typography
- Footer renders with minimal links
- Admin pages completely unaffected
- shadcn/ui card, separator, aspect-ratio components installed
</success_criteria>

<output>
After completion, create `.planning/phases/03-catalog-storefront-seed-data/03-02-SUMMARY.md`
</output>
