---
phase: 32-kustomize-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/k8s/base/kustomization.yaml
  - infra/k8s/base/keycloak/deployment.yaml
  - infra/k8s/base/rabbitmq/deployment.yaml
  - infra/k8s/base/postgres/statefulset.yaml
  - infra/k8s/base/rabbitmq/service.yaml
  - infra/k8s/base/keycloak/service.yaml
  - infra/k8s/base/postgres/service.yaml
  - infra/k8s/overlays/dev/kustomization.yaml
  - infra/k8s/overlays/dev/apiservice/kustomization.yaml
  - infra/k8s/overlays/dev/gateway/kustomization.yaml
  - infra/k8s/overlays/dev/web/kustomization.yaml
autonomous: true
requirements:
  - KUST-01
  - KUST-02

must_haves:
  truths:
    - "No base manifest contains a hardcoded namespace: field — Kustomize namespace transformer handles it"
    - "otel-collector and aspire-dashboard resources only appear in the dev overlay, not in base kustomization.yaml"
    - "kustomize build on the dev overlay still produces all 8 workloads with namespace micro-commerce"
  artifacts:
    - path: "infra/k8s/base/kustomization.yaml"
      provides: "Base kustomization without otel-collector and aspire-dashboard"
      contains: "namespace: micro-commerce"
    - path: "infra/k8s/overlays/dev/kustomization.yaml"
      provides: "Dev overlay referencing otel-collector and aspire-dashboard"
      contains: "otel-collector"
  key_links:
    - from: "infra/k8s/overlays/dev/kustomization.yaml"
      to: "infra/k8s/base/otel-collector/"
      via: "Kustomize resource reference"
      pattern: "otel-collector"
    - from: "infra/k8s/overlays/dev/kustomization.yaml"
      to: "infra/k8s/base/aspire-dashboard/"
      via: "Kustomize resource reference"
      pattern: "aspire-dashboard"
---

<objective>
Remove hardcoded namespaces from base manifests and move dev-only resources to the dev overlay.

Purpose: Kustomize convention is to set namespace via the namespace transformer in kustomization.yaml, not hardcode it into individual manifests. Additionally, otel-collector and aspire-dashboard are dev-only tools and belong in the dev overlay, not the base.

Output: Clean base manifests with no hardcoded namespaces; dev overlay includes otel-collector and aspire-dashboard.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove hardcoded namespace fields from all base manifests</name>
  <files>
    infra/k8s/base/keycloak/deployment.yaml
    infra/k8s/base/rabbitmq/deployment.yaml
    infra/k8s/base/postgres/statefulset.yaml
    infra/k8s/base/rabbitmq/service.yaml
    infra/k8s/base/keycloak/service.yaml
    infra/k8s/base/postgres/service.yaml
    infra/k8s/overlays/dev/apiservice/kustomization.yaml
    infra/k8s/overlays/dev/gateway/kustomization.yaml
    infra/k8s/overlays/dev/web/kustomization.yaml
  </files>
  <action>
Remove every `namespace: micro-commerce` line from base manifest metadata blocks. The base `kustomization.yaml` already has `namespace: micro-commerce` which sets namespace via the Kustomize transformer.

Files that need `namespace:` removed from metadata:
- `infra/k8s/base/keycloak/deployment.yaml` — line 5
- `infra/k8s/base/rabbitmq/deployment.yaml` — line 5
- `infra/k8s/base/postgres/statefulset.yaml` — line 5
- `infra/k8s/base/rabbitmq/service.yaml` — lines 5 and 21 (two Service resources in one file)
- `infra/k8s/base/keycloak/service.yaml` — lines 5 and 18 (two Service resources in one file)
- `infra/k8s/base/postgres/service.yaml` — lines 6 and 20 (two Service resources in one file)

Also remove redundant `namespace: micro-commerce` from overlay kustomization.yaml files (the base already sets it, overlays inherit):
- `infra/k8s/overlays/dev/apiservice/kustomization.yaml` — line 4
- `infra/k8s/overlays/dev/gateway/kustomization.yaml` — line 4
- `infra/k8s/overlays/dev/web/kustomization.yaml` — line 4

Do NOT remove `namespace: micro-commerce` from `infra/k8s/base/kustomization.yaml` — that is the Kustomize transformer and must stay.
Do NOT remove `namespace: micro-commerce` from `infra/k8s/overlays/dev/kustomization.yaml` — that is also a Kustomize transformer.
Do NOT touch ArgoCD manifests (`infra/k8s/argocd/`) — those use `namespace: argocd` which is correct for ArgoCD resources.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/micro-commerce && grep -rn "namespace: micro-commerce" infra/k8s/base/ --include="*.yaml" | grep -v kustomization.yaml | wc -l | xargs test 0 -eq && echo "PASS: No hardcoded namespaces in base manifests" || echo "FAIL: Hardcoded namespaces still present"</automated>
  </verify>
  <done>Zero base manifest files (deployment, service, statefulset) contain hardcoded namespace fields. Only kustomization.yaml files retain the namespace transformer.</done>
</task>

<task type="auto">
  <name>Task 2: Move otel-collector and aspire-dashboard from base to dev overlay</name>
  <files>
    infra/k8s/base/kustomization.yaml
    infra/k8s/overlays/dev/kustomization.yaml
  </files>
  <action>
Edit `infra/k8s/base/kustomization.yaml`:
- Remove `- otel-collector/` and `- aspire-dashboard/` from the resources list
- Keep all other resources (namespace.yaml, postgres/, rabbitmq/, keycloak/, apiservice/, gateway/, web/)

Edit `infra/k8s/overlays/dev/kustomization.yaml`:
- Add `- ../../base/otel-collector` and `- ../../base/aspire-dashboard` to the resources list
- The existing resources (../../base/postgres, ../../base/rabbitmq, ../../base/keycloak, apiservice/, gateway/, web/) stay unchanged

The base otel-collector and aspire-dashboard directories (deployment.yaml, service.yaml, kustomization.yaml, configmap.yaml) stay where they are — they are just no longer referenced from base kustomization.yaml but instead from the dev overlay.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/micro-commerce && grep -c "otel-collector\|aspire-dashboard" infra/k8s/base/kustomization.yaml | xargs test 0 -eq && grep -c "otel-collector" infra/k8s/overlays/dev/kustomization.yaml | xargs test 0 -lt && grep -c "aspire-dashboard" infra/k8s/overlays/dev/kustomization.yaml | xargs test 0 -lt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>otel-collector and aspire-dashboard are not listed in base/kustomization.yaml but are listed in overlays/dev/kustomization.yaml. Base is production-ready; dev overlay adds observability tooling.</done>
</task>

</tasks>

<verification>
Run `kustomize build` (or `kubectl kustomize`) on the dev overlay to confirm all resources render correctly with the micro-commerce namespace:

```bash
cd infra/k8s && kubectl kustomize overlays/dev/ 2>&1 | head -5
```

If kubectl is not available, verify structurally:
```bash
grep -rn "namespace:" infra/k8s/base/ --include="*.yaml" | grep -v kustomization.yaml
# Should return zero lines

grep "otel-collector\|aspire-dashboard" infra/k8s/base/kustomization.yaml
# Should return zero lines

grep "otel-collector\|aspire-dashboard" infra/k8s/overlays/dev/kustomization.yaml
# Should return two lines
```
</verification>

<success_criteria>
- Zero hardcoded `namespace:` in any base deployment, service, or statefulset YAML
- otel-collector and aspire-dashboard appear only in dev overlay resources
- Kustomize namespace transformer remains in base and dev overlay kustomization.yaml
- All base manifest files are valid YAML
</success_criteria>

<output>
After completion, create `.planning/phases/32-kustomize-hygiene/32-01-SUMMARY.md`
</output>
