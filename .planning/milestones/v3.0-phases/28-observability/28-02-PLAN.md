---
phase: 28-observability
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - infra/k8s/base/apiservice/deployment.yaml
  - infra/k8s/base/gateway/deployment.yaml
  - infra/k8s/argocd/apps/otel-collector.yaml
  - infra/k8s/argocd/apps/aspire-dashboard.yaml
  - infra/k8s/kind-config.yaml
autonomous: true
requirements: [OBSV-01, OBSV-02]

must_haves:
  truths:
    - "ApiService and Gateway have OTEL_EXPORTER_OTLP_ENDPOINT and OTEL_SERVICE_NAME env vars set to send telemetry to the OTEL Collector"
    - "ArgoCD app-of-apps discovers and manages both new observability services"
    - "Aspire Dashboard UI is reachable from the host browser at localhost:38888 via kind port mapping"
  artifacts:
    - path: "infra/k8s/base/apiservice/deployment.yaml"
      provides: "OTEL env vars on ApiService"
      contains: "OTEL_EXPORTER_OTLP_ENDPOINT"
    - path: "infra/k8s/base/gateway/deployment.yaml"
      provides: "OTEL env vars on Gateway"
      contains: "OTEL_EXPORTER_OTLP_ENDPOINT"
    - path: "infra/k8s/argocd/apps/otel-collector.yaml"
      provides: "ArgoCD Application for OTEL Collector"
      contains: "path: infra/k8s/base/otel-collector"
    - path: "infra/k8s/argocd/apps/aspire-dashboard.yaml"
      provides: "ArgoCD Application for Aspire Dashboard"
      contains: "path: infra/k8s/base/aspire-dashboard"
    - path: "infra/k8s/kind-config.yaml"
      provides: "Port mapping for Dashboard NodePort 30888 -> host 38888"
      contains: "hostPort: 38888"
  key_links:
    - from: "infra/k8s/base/apiservice/deployment.yaml"
      to: "otel-collector:4317"
      via: "OTEL_EXPORTER_OTLP_ENDPOINT env var"
      pattern: "http://otel-collector:4317"
    - from: "infra/k8s/base/gateway/deployment.yaml"
      to: "otel-collector:4317"
      via: "OTEL_EXPORTER_OTLP_ENDPOINT env var"
      pattern: "http://otel-collector:4317"
    - from: "infra/k8s/argocd/apps/otel-collector.yaml"
      to: "infra/k8s/base/otel-collector"
      via: "ArgoCD source path"
      pattern: "infra/k8s/base/otel-collector"
    - from: "infra/k8s/kind-config.yaml"
      to: "Aspire Dashboard NodePort 30888"
      via: "kind extraPortMappings"
      pattern: "containerPort: 30888"
---

<objective>
Wire OTEL telemetry from application services to the Collector, register new services with ArgoCD, and add kind port mapping for Dashboard browser access.

Purpose: Connect the observability pipeline end-to-end. Without this plan, the OTEL Collector and Dashboard manifests from Plan 01 exist but receive no telemetry, are not managed by ArgoCD, and the Dashboard UI is unreachable from the host.

Output: 2 modified Deployment YAMLs, 2 new ArgoCD Application YAMLs, 1 updated kind-config.yaml.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-observability/28-RESEARCH.md
@.planning/phases/28-observability/28-01-SUMMARY.md

Existing ArgoCD Application pattern (infrastructure services point to base/, not overlays):
```yaml
# infra/k8s/argocd/apps/postgres.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgres
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/baotoq/micro-commerce.git
    targetRevision: master
    path: infra/k8s/base/postgres
  destination:
    server: https://kubernetes.default.svc
    namespace: micro-commerce
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
    - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
```

Existing kind-config.yaml port mapping pattern:
```yaml
  # ArgoCD UI (NodePort 30443 -> host 38443)
  - containerPort: 30443
    hostPort: 38443
    protocol: TCP
```

Existing ApiService env vars (append OTEL vars AFTER the last existing env var):
```yaml
env:
- name: ASPNETCORE_ENVIRONMENT
  value: Production
# ... (connection strings, MassTransit, etc.)
- name: ConnectionStrings__keycloak
  value: "http://keycloak.micro-commerce.svc.cluster.local:8080"
```

ServiceDefaults already conditionally enables OTLP export when env var is set:
```csharp
var useOtlpExporter = !string.IsNullOrWhiteSpace(builder.Configuration["OTEL_EXPORTER_OTLP_ENDPOINT"]);
if (useOtlpExporter) { builder.Services.AddOpenTelemetry().UseOtlpExporter(); }
```
No application code changes needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OTEL env vars to ApiService and Gateway deployments</name>
  <files>
    infra/k8s/base/apiservice/deployment.yaml,
    infra/k8s/base/gateway/deployment.yaml
  </files>
  <action>
Add two environment variables to each Deployment's container env section.

**infra/k8s/base/apiservice/deployment.yaml** -- append after the last existing env var (after `ConnectionStrings__keycloak`):
```yaml
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector:4317"
        - name: OTEL_SERVICE_NAME
          value: "apiservice"
```

**infra/k8s/base/gateway/deployment.yaml** -- append after the last existing env var (after `Cors__Origins__0`):
```yaml
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector:4317"
        - name: OTEL_SERVICE_NAME
          value: "gateway"
```

CRITICAL: `OTEL_SERVICE_NAME` MUST be set. Without it, all traces appear as "unknown_service" in the Dashboard, making ApiService indistinguishable from Gateway. Use lowercase names matching Aspire AppHost convention.

The existing ServiceDefaults code already checks `OTEL_EXPORTER_OTLP_ENDPOINT` and enables the OTLP exporter when set. No application code changes are needed.
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/micro-commerce && grep "OTEL_EXPORTER_OTLP_ENDPOINT" infra/k8s/base/apiservice/deployment.yaml && grep "OTEL_SERVICE_NAME" infra/k8s/base/apiservice/deployment.yaml && grep "otel-collector:4317" infra/k8s/base/apiservice/deployment.yaml && grep "OTEL_EXPORTER_OTLP_ENDPOINT" infra/k8s/base/gateway/deployment.yaml && grep "OTEL_SERVICE_NAME" infra/k8s/base/gateway/deployment.yaml && grep "otel-collector:4317" infra/k8s/base/gateway/deployment.yaml</automated>
  </verify>
  <done>Both apiservice and gateway Deployment YAMLs contain OTEL_EXPORTER_OTLP_ENDPOINT pointing to http://otel-collector:4317 and OTEL_SERVICE_NAME set to "apiservice" and "gateway" respectively.</done>
</task>

<task type="auto">
  <name>Task 2: Create ArgoCD Applications and update kind-config</name>
  <files>
    infra/k8s/argocd/apps/otel-collector.yaml,
    infra/k8s/argocd/apps/aspire-dashboard.yaml,
    infra/k8s/kind-config.yaml
  </files>
  <action>
**infra/k8s/argocd/apps/otel-collector.yaml:**
Follow the exact pattern from `postgres.yaml` (infrastructure services point to base/ directories, not overlays):
- metadata.name: otel-collector
- metadata.namespace: argocd
- finalizers: resources-finalizer.argocd.argoproj.io
- spec.source.repoURL: https://github.com/baotoq/micro-commerce.git
- spec.source.targetRevision: master
- spec.source.path: infra/k8s/base/otel-collector
- spec.destination.server: https://kubernetes.default.svc
- spec.destination.namespace: micro-commerce
- syncPolicy: automated (selfHeal: true, prune: true), CreateNamespace=true, retry (limit 5, backoff 5s/2x/3m)

**infra/k8s/argocd/apps/aspire-dashboard.yaml:**
Same pattern as otel-collector.yaml but with:
- metadata.name: aspire-dashboard
- spec.source.path: infra/k8s/base/aspire-dashboard

These use base/ paths (not overlays) because they use fixed public images -- no CI/CD image tag patching needed.

**infra/k8s/kind-config.yaml:**
Add a new port mapping entry after the ArgoCD entry:
```yaml
  # Aspire Dashboard (NodePort 30888 -> host 38888)
  - containerPort: 30888
    hostPort: 38888
    protocol: TCP
```

NOTE: Kind does not support hot-adding port mappings. Existing clusters must be recreated for this to take effect (`kind delete cluster --name micro-commerce && ./infra/k8s/bootstrap.sh`).
  </action>
  <verify>
    <automated>cd /Users/baotoq/Work/micro-commerce && grep "path: infra/k8s/base/otel-collector" infra/k8s/argocd/apps/otel-collector.yaml && grep "path: infra/k8s/base/aspire-dashboard" infra/k8s/argocd/apps/aspire-dashboard.yaml && grep "hostPort: 38888" infra/k8s/kind-config.yaml && grep "containerPort: 30888" infra/k8s/kind-config.yaml</automated>
  </verify>
  <done>Two ArgoCD Application YAMLs exist in infra/k8s/argocd/apps/ pointing to base directories. kind-config.yaml has port mapping 30888->38888 for Aspire Dashboard browser access. ArgoCD root app-of-apps (which watches the apps/ directory) will auto-discover both new Applications on next sync.</done>
</task>

</tasks>

<verification>
End-to-end telemetry pipeline wired:
1. ApiService/Gateway -> OTEL Collector: `grep "otel-collector:4317" infra/k8s/base/apiservice/deployment.yaml infra/k8s/base/gateway/deployment.yaml`
2. ArgoCD manages new services: `ls infra/k8s/argocd/apps/otel-collector.yaml infra/k8s/argocd/apps/aspire-dashboard.yaml`
3. Dashboard reachable from host: `grep "38888" infra/k8s/kind-config.yaml`

Combined with Plan 01 verification:
- OTEL Collector config forwards to aspire-dashboard:18889
- Aspire Dashboard has anonymous auth enabled
- All manifests are Kustomize-discoverable
</verification>

<success_criteria>
- ApiService and Gateway Deployments have OTEL_EXPORTER_OTLP_ENDPOINT and OTEL_SERVICE_NAME env vars
- Two ArgoCD Application YAMLs exist for otel-collector and aspire-dashboard, following the infrastructure (base/) pattern
- kind-config.yaml maps NodePort 30888 to host port 38888 for Dashboard browser access
- The full telemetry chain is wired: Apps -> Collector -> Dashboard
- No application code changes -- only K8s manifest modifications
</success_criteria>

<output>
After completion, create `.planning/phases/28-observability/28-02-SUMMARY.md`
</output>
