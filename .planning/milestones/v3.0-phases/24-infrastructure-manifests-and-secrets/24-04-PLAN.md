---
phase: 24-infrastructure-manifests-and-secrets
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [INFRA-05, GOPS-03]

must_haves:
  truths:
    - "Phase 24 success criteria accurately reflect infrastructure-only scope (no application manifests)"
    - "INFRA-05 scope is explicitly split: Keycloak startup probe in Phase 24, ApiService startup probe deferred to Phase 25"
    - "GOPS-03 acknowledges runtime-generated sealed-secret.yaml files as by-design (committed after first bootstrap)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 24 success criteria and Phase 25 success criteria that includes ApiService startup probe"
      contains: "ApiService startup probe"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INFRA-05 split notation between Phase 24 and Phase 25"
      contains: "INFRA-05"
  key_links:
    - from: ".planning/ROADMAP.md Phase 24 success criteria"
      to: ".planning/ROADMAP.md Phase 25 success criteria"
      via: "ApiService startup probe moved from criterion #5 to Phase 25 criteria"
      pattern: "startup probe"
---

<objective>
Fix ROADMAP misalignment where Phase 24 success criterion #5 references an ApiService startup probe that belongs to Phase 25 (application manifests), and clarify INFRA-05/GOPS-03 scope documentation.

Purpose: Verification found that success criterion #5 ("ApiService startup probe prevents liveness failure during slow first-boot EF Core migration") cannot be satisfied by Phase 24 because no ApiService K8s manifest exists here — only infrastructure services (PostgreSQL, RabbitMQ, Keycloak). The ApiService manifest is Phase 25 scope (MFST-01, MFST-02). This plan corrects the documentation to reflect actual deliverable boundaries.

Output: Updated ROADMAP.md and REQUIREMENTS.md with accurate phase scope boundaries.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-infrastructure-manifests-and-secrets/24-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP.md Phase 24 success criteria and add ApiService startup probe to Phase 25</name>
  <files>.planning/ROADMAP.md</files>
  <action>
In `.planning/ROADMAP.md`, locate the Phase 24 success criteria section and make these changes:

1. **Phase 24 success criterion #5** — Replace:
   ```
   5. ApiService startup probe prevents liveness failure during the slow first-boot EF Core migration
   ```
   With:
   ```
   5. Keycloak startup probe is configured with sufficient timeout (15s delay + 60s probing window) to survive slow first-boot realm import
   ```
   This accurately describes what Phase 24 actually delivers. The Keycloak startup probe IS implemented in `infra/k8s/base/keycloak/deployment.yaml`.

2. **Phase 25 success criteria** — Add a new criterion about ApiService startup probe. After the existing success criteria for Phase 25, add:
   ```
   6. ApiService startup probe prevents liveness failure during slow first-boot EF Core migration (deferred from Phase 24 INFRA-05)
   ```

3. **Phase 24 Plans section** — Update plan count to 4 and add the gap closure plan entry:
   ```
   - [x] 24-04-PLAN.md -- Gap closure: fix ROADMAP misalignment on success criterion #5
   ```

Do NOT change Phase 24 requirements line — INFRA-05 stays listed there because the Keycloak half was delivered.
  </action>
  <verify>
    <automated>grep -n "Keycloak startup probe" .planning/ROADMAP.md | head -5 && grep -n "ApiService startup probe" .planning/ROADMAP.md | head -5</automated>
  </verify>
  <done>Phase 24 criterion #5 describes Keycloak startup probe (actually delivered), Phase 25 has a new criterion for ApiService startup probe (deferred from Phase 24)</done>
</task>

<task type="auto">
  <name>Task 2: Clarify INFRA-05 and GOPS-03 scope in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In `.planning/REQUIREMENTS.md`, make these changes:

1. **INFRA-05** — Update the description to clarify the scope split. Change:
   ```
   - [x] **INFRA-05**: Startup probes protect Keycloak and ApiService from premature liveness failures
   ```
   To:
   ```
   - [x] **INFRA-05**: Startup probes protect Keycloak (Phase 24) and ApiService (Phase 25) from premature liveness failures
   ```
   This keeps the requirement checked (both halves ARE planned) while making the phase split explicit.

2. **GOPS-03** — Update the description to note the runtime-generation workflow. Change:
   ```
   - [x] **GOPS-03**: Sealed Secrets controller encrypts all credentials; SealedSecret YAMLs committed to Git
   ```
   To:
   ```
   - [x] **GOPS-03**: Sealed Secrets controller encrypts all credentials; SealedSecret YAMLs generated by bootstrap script and committed to Git after first run
   ```
   This clarifies that sealed-secret.yaml files are intentionally runtime-generated (each cluster has a unique key pair) and committed post-bootstrap.

3. **Traceability table** — Add Phase 25 entry for the INFRA-05 ApiService half. After the `INFRA-05 | Phase 24 | Complete` row, add:
   ```
   | INFRA-05 | Phase 24 + 25 | Partial (Keycloak done, ApiService in Phase 25) |
   ```
   Actually, simpler: change the existing INFRA-05 row from `Phase 24 | Complete` to `Phase 24 + 25 | Partial (Keycloak done P24, ApiService P25)`.

Do NOT change any other requirements.
  </action>
  <verify>
    <automated>grep -n "INFRA-05" .planning/REQUIREMENTS.md && grep -n "GOPS-03" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>INFRA-05 shows phase split (P24 Keycloak, P25 ApiService), GOPS-03 clarifies runtime-generation workflow</done>
</task>

</tasks>

<verification>
1. `grep "Keycloak startup probe" .planning/ROADMAP.md` returns Phase 24 criterion
2. `grep "ApiService startup probe" .planning/ROADMAP.md` returns Phase 25 criterion
3. `grep "Phase 24 + 25" .planning/REQUIREMENTS.md` returns INFRA-05 traceability entry
4. `grep "bootstrap script" .planning/REQUIREMENTS.md` returns GOPS-03 clarification
</verification>

<success_criteria>
- Phase 24 success criteria accurately describe infrastructure-only deliverables (Keycloak startup probe, not ApiService)
- Phase 25 explicitly inherits the ApiService startup probe responsibility
- INFRA-05 and GOPS-03 descriptions clarify scope boundaries without changing completion status
- No other requirements or phase content modified
</success_criteria>

<output>
After completion, create `.planning/phases/24-infrastructure-manifests-and-secrets/24-04-SUMMARY.md`
</output>
