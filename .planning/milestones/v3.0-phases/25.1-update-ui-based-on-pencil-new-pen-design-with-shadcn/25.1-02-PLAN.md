---
phase: 25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn
plan: 02
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - src/MicroCommerce.Web/src/components/storefront/header.tsx
  - src/MicroCommerce.Web/src/components/storefront/footer.tsx
  - src/MicroCommerce.Web/src/components/storefront/hero-banner.tsx
  - src/MicroCommerce.Web/src/components/storefront/product-card.tsx
  - src/MicroCommerce.Web/src/components/storefront/category-pill.tsx
  - src/MicroCommerce.Web/src/app/(storefront)/page.tsx
autonomous: true
requirements:
  - UI-HOME
must_haves:
  truths:
    - "Homepage displays a gradient hero section (blue-to-green, 135deg)"
    - "Category pills with pill shape (rounded-full) and active state are visible"
    - "Featured products show in a responsive grid using styled ProductCard components"
    - "Header has logo, search, cart icon, and auth controls"
    - "Footer has multi-column links layout"
  artifacts:
    - path: "src/MicroCommerce.Web/src/components/storefront/hero-banner.tsx"
      provides: "Hero section with gradient background matching design"
    - path: "src/MicroCommerce.Web/src/components/storefront/category-pill.tsx"
      provides: "New CategoryPill component (pill shape, icon + label, active/inactive state)"
    - path: "src/MicroCommerce.Web/src/components/storefront/product-card.tsx"
      provides: "Restyled ProductCard matching design"
    - path: "src/MicroCommerce.Web/src/components/storefront/header.tsx"
      provides: "Restyled header with design layout"
    - path: "src/MicroCommerce.Web/src/components/storefront/footer.tsx"
      provides: "Restyled footer with multi-column links"
  key_links:
    - from: "src/MicroCommerce.Web/src/app/(storefront)/page.tsx"
      to: "src/MicroCommerce.Web/src/components/storefront/hero-banner.tsx"
      via: "import and render"
      pattern: "HeroBanner"
    - from: "src/MicroCommerce.Web/src/app/(storefront)/page.tsx"
      to: "src/MicroCommerce.Web/src/components/storefront/category-pill.tsx"
      via: "import and render in categories section"
      pattern: "CategoryPill"
---

<objective>
Restyle the homepage to match design/storefront-homepage.pen: gradient hero, category pills, featured product grid, header, and footer.

Purpose: Homepage is the first screen users see. Sets the visual tone for the entire storefront.
Output: Rewritten hero-banner, header, footer, product-card components + new category-pill component.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-CONTEXT.md
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-RESEARCH.md
@design/storefront-homepage.pen
@design/design-system.pen
@src/MicroCommerce.Web/src/app/(storefront)/page.tsx
@src/MicroCommerce.Web/src/components/storefront/hero-banner.tsx
@src/MicroCommerce.Web/src/components/storefront/header.tsx
@src/MicroCommerce.Web/src/components/storefront/footer.tsx
@src/MicroCommerce.Web/src/components/storefront/product-card.tsx
@src/MicroCommerce.Web/src/app/(storefront)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CategoryPill component and restyle header, footer, hero-banner</name>
  <files>
    src/MicroCommerce.Web/src/components/storefront/category-pill.tsx,
    src/MicroCommerce.Web/src/components/storefront/header.tsx,
    src/MicroCommerce.Web/src/components/storefront/footer.tsx,
    src/MicroCommerce.Web/src/components/storefront/hero-banner.tsx
  </files>
  <action>
1. **Create CategoryPill** (`src/components/storefront/category-pill.tsx`):
   - New file, not in shadcn. Use the RESEARCH.md "Example 3: CategoryPill Custom Component" as template.
   - Props: `label: string`, `icon?: LucideIcon`, `active?: boolean`, `onClick?: () => void`
   - Styling: `rounded-full`, `px-4 py-2`, `text-sm font-medium`, active=`bg-primary text-primary-foreground`, inactive=`bg-secondary text-secondary-foreground hover:bg-secondary/80`
   - Import `cn` from `@/lib/utils`

2. **Restyle header.tsx**: Read the design/storefront-homepage.pen header section. Rewrite JSX to match:
   - Logo text "MicroCommerce" on left
   - Central search bar (use existing SearchBar or inline input with Search icon)
   - Right side: cart icon with badge count, user avatar/login button
   - Use `bg-background border-b border-border` for header container
   - Keep existing data hooks (useCart, useSession) and navigation logic unchanged
   - Add responsive: hamburger menu on mobile using shadcn Sheet

3. **Restyle footer.tsx**: Read the design/storefront-homepage.pen footer section. Rewrite JSX to match:
   - Multi-column grid layout (4 columns on desktop, 2 on tablet, 1 on mobile)
   - Sections: Company info, Shop links, Customer Service links, Connect/social
   - Bottom bar with copyright
   - Use `bg-secondary text-secondary-foreground` for footer background
   - Keep any existing link data, just restyle the layout

4. **Restyle hero-banner.tsx**: Read the design/storefront-homepage.pen hero section. Rewrite JSX to match:
   - Gradient background: `bg-gradient-to-br from-blue-600 to-green-500` (or custom gradient matching design)
   - Large heading text in white, subheading, CTA button
   - Keep existing props/data unchanged, only restyle JSX and classes

CRITICAL: Preserve all existing prop interfaces, data hooks, and navigation logic. Only rewrite JSX structure and Tailwind classes.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && test -f src/components/storefront/category-pill.tsx && echo "PASS: CategoryPill exists" && grep -q "rounded-full" src/components/storefront/category-pill.tsx && echo "PASS: Pill shape" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>CategoryPill component created with pill shape and active/inactive states. Header, footer, and hero-banner restyled to match design/storefront-homepage.pen layout.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ProductCard and update homepage page.tsx</name>
  <files>
    src/MicroCommerce.Web/src/components/storefront/product-card.tsx,
    src/MicroCommerce.Web/src/app/(storefront)/page.tsx
  </files>
  <action>
1. **Restyle product-card.tsx**: Read design/storefront-homepage.pen and design/design-system.pen ProductCard component spec.
   - Card with image area, product name, price, optional rating stars, add-to-cart button
   - Use shadcn Card component as base (`<Card>`, `<CardContent>`)
   - Image area with hover effect (subtle scale/shadow transition)
   - Price in `font-semibold`, strike-through for original price if on sale
   - Rating display with star icons using `text-star` color
   - Keep existing props interface (product data, onAddToCart, etc.) unchanged
   - Keep existing hooks and data fetching unchanged

2. **Update page.tsx (homepage)**: Read design/storefront-homepage.pen for full page layout. Update to include:
   - Hero section (HeroBanner component)
   - Category pills section (horizontal scrollable row of CategoryPill components)
   - Featured products section heading + ProductGrid
   - Section spacing using consistent `py-12` or `py-16` sections
   - `max-w-7xl mx-auto px-4` container pattern
   - Keep all existing data fetching (server component fetch or hooks) unchanged
   - Only restructure the JSX layout and add CategoryPill integration

CRITICAL: Do NOT modify any hooks, API calls, or data fetching logic. Only change JSX structure and styling.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && grep -q "CategoryPill\|category-pill" src/app/\(storefront\)/page.tsx && echo "PASS: CategoryPill used in homepage" && grep -q "Card" src/components/storefront/product-card.tsx && echo "PASS: shadcn Card used" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>ProductCard restyled with shadcn Card, image hover effects, star rating color. Homepage page.tsx restructured with hero, category pills row, and featured products sections matching design.</done>
</task>

</tasks>

<verification>
- Homepage renders with gradient hero, category pills, and product grid
- ProductCard shows image, name, price, and optional rating
- Header shows logo, search, cart, auth controls
- Footer shows multi-column layout
- `npm run lint` passes
</verification>

<success_criteria>
1. CategoryPill component exists with pill shape and active/inactive variants
2. Hero banner has gradient background (blue-to-green)
3. Product cards use shadcn Card with consistent styling
4. Header and footer match the design/storefront-homepage.pen layout
5. All existing functionality preserved (navigation, cart, auth)
6. Biome lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-02-SUMMARY.md`
</output>
