---
phase: 25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn
plan: 06
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - src/MicroCommerce.Web/src/components/storefront/checkout-page.tsx
  - src/MicroCommerce.Web/src/components/storefront/checkout-accordion.tsx
  - src/MicroCommerce.Web/src/components/storefront/checkout-login-gate.tsx
  - src/MicroCommerce.Web/src/components/storefront/shipping-section.tsx
  - src/MicroCommerce.Web/src/components/storefront/payment-section.tsx
  - src/MicroCommerce.Web/src/components/storefront/order-sidebar.tsx
  - src/MicroCommerce.Web/src/components/storefront/step-indicator.tsx
  - src/MicroCommerce.Web/src/components/ui/radio-group.tsx
autonomous: true
requirements:
  - UI-CHECKOUT
must_haves:
  truths:
    - "Checkout page shows step indicator (Shipping -> Payment -> Review)"
    - "Shipping section has address form fields and shipping method radio selection"
    - "Payment section has card input fields"
    - "Order summary sidebar shows items, subtotal, shipping, tax, total"
    - "Step progression works (next/back between steps)"
  artifacts:
    - path: "src/MicroCommerce.Web/src/components/storefront/step-indicator.tsx"
      provides: "New StepIndicator component (active/completed/upcoming steps)"
    - path: "src/MicroCommerce.Web/src/components/ui/radio-group.tsx"
      provides: "shadcn radio-group component"
    - path: "src/MicroCommerce.Web/src/components/storefront/checkout-page.tsx"
      provides: "Restyled checkout layout with step indicator"
  key_links:
    - from: "src/MicroCommerce.Web/src/components/storefront/checkout-page.tsx"
      to: "src/MicroCommerce.Web/src/components/storefront/step-indicator.tsx"
      via: "import StepIndicator at top of checkout flow"
      pattern: "StepIndicator"
    - from: "src/MicroCommerce.Web/src/components/storefront/shipping-section.tsx"
      to: "src/MicroCommerce.Web/src/components/ui/radio-group.tsx"
      via: "RadioGroup for shipping method selection"
      pattern: "RadioGroup"
---

<objective>
Restyle the checkout flow with step indicator, shipping/payment sections, and order summary sidebar matching design/storefront-checkout.pen.

Purpose: Checkout is the highest-value conversion page. Clear step progression and organized form sections reduce abandonment.
Output: New StepIndicator component, installed radio-group, restyled checkout components.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-CONTEXT.md
@.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-RESEARCH.md
@design/storefront-checkout.pen
@design/design-system.pen
@src/MicroCommerce.Web/src/components/storefront/checkout-page.tsx
@src/MicroCommerce.Web/src/components/storefront/checkout-accordion.tsx
@src/MicroCommerce.Web/src/components/storefront/checkout-login-gate.tsx
@src/MicroCommerce.Web/src/components/storefront/shipping-section.tsx
@src/MicroCommerce.Web/src/components/storefront/payment-section.tsx
@src/MicroCommerce.Web/src/components/storefront/order-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install radio-group, create StepIndicator, restyle checkout-page and checkout-login-gate</name>
  <files>
    src/MicroCommerce.Web/src/components/ui/radio-group.tsx,
    src/MicroCommerce.Web/src/components/storefront/step-indicator.tsx,
    src/MicroCommerce.Web/src/components/storefront/checkout-page.tsx,
    src/MicroCommerce.Web/src/components/storefront/checkout-login-gate.tsx
  </files>
  <action>
1. **Install shadcn radio-group** (run from src/MicroCommerce.Web):
   ```
   npx shadcn@latest add radio-group
   ```

2. **Create StepIndicator** (`src/components/storefront/step-indicator.tsx`):
   - Custom component (not in shadcn) per design-system.pen StepIndicator spec
   - Props: `steps: { label: string; description?: string }[]`, `currentStep: number`
   - Horizontal layout: each step shows number circle + label + connecting line
   - States: completed (check icon, primary bg), active (primary border, primary text), upcoming (muted border, muted text)
   - Connecting line between steps: completed = primary color, upcoming = muted color
   - Responsive: on mobile, simplify to "Step 1 of 3: Shipping" text

3. **Restyle checkout-page.tsx**: Read design/storefront-checkout.pen layout.
   - Page heading: "Checkout"
   - StepIndicator at top showing Shipping -> Payment -> Review steps
   - Two-column layout: left = form sections (wider), right = order summary sidebar (sticky)
   - Step content area shows the active step's section (shipping, payment, or review)
   - Navigation: "Continue" / "Back" buttons at bottom of each step
   - Container: `max-w-7xl mx-auto px-4 py-8`
   - Keep existing step state management and checkout logic unchanged

4. **Restyle checkout-login-gate.tsx**: Update styling:
   - Card-style container prompting login before checkout
   - "Sign in to continue" heading, auth button, "or continue as guest" option
   - Use shadcn Card and Button components
   - Keep existing auth logic unchanged

CRITICAL: Preserve all checkout state (step navigation, form data, submission) — only restyle.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && test -f src/components/ui/radio-group.tsx && test -f src/components/storefront/step-indicator.tsx && echo "PASS: Components exist" && grep -q "StepIndicator" src/components/storefront/checkout-page.tsx && echo "PASS: StepIndicator used" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Radio-group installed. StepIndicator created with completed/active/upcoming states. Checkout page restyled with step progression and two-column layout.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle shipping, payment, order-sidebar, and checkout-accordion</name>
  <files>
    src/MicroCommerce.Web/src/components/storefront/shipping-section.tsx,
    src/MicroCommerce.Web/src/components/storefront/payment-section.tsx,
    src/MicroCommerce.Web/src/components/storefront/order-sidebar.tsx,
    src/MicroCommerce.Web/src/components/storefront/checkout-accordion.tsx
  </files>
  <action>
1. **Restyle shipping-section.tsx**: Read design/storefront-checkout.pen shipping section.
   - Form fields: First Name, Last Name, Email, Phone, Address Line 1, Address Line 2, City, State, Zip, Country
   - Use shadcn Input and Label for form fields, grid layout (2 columns for name, city/state/zip)
   - Shipping method selection: RadioGroup with options (Standard, Express, etc.), each showing name + estimated time + price
   - Keep existing form state, validation, and onChange handlers

2. **Restyle payment-section.tsx**: Read design/storefront-checkout.pen payment section.
   - Card number, expiry, CVC fields with appropriate icons
   - Use shadcn Input and Label
   - Saved payment method selection if applicable
   - Keep existing payment logic unchanged

3. **Restyle order-sidebar.tsx**: Read design/storefront-checkout.pen sidebar.
   - Card container showing: list of items (thumbnail + name + qty + price), subtotal, shipping, tax, divider, total
   - Similar to cart-summary but read-only (no edit controls)
   - Sticky positioning on scroll
   - Keep existing data mapping unchanged

4. **Restyle checkout-accordion.tsx**: If still used (the step indicator may replace accordion pattern), update to match design. If accordion is no longer the primary navigation pattern (steps replace it), simplify or adapt to serve as a collapsible review section within the "Review" step.

CRITICAL: Keep all form validation, submission handlers, and checkout state management unchanged.
  </action>
  <verify>
    <automated>cd src/MicroCommerce.Web && grep -q "RadioGroup\|radio-group" src/components/storefront/shipping-section.tsx && echo "PASS: RadioGroup in shipping" && grep -q "Input\|input" src/components/storefront/payment-section.tsx && echo "PASS: Input in payment" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Shipping section uses RadioGroup for method selection. Payment section has styled card fields. Order sidebar is a sticky card with item summary. All form logic preserved.</done>
</task>

</tasks>

<verification>
- Checkout page shows 3-step indicator at top
- Shipping step has address fields and radio shipping method selection
- Payment step has card input fields
- Order summary sidebar is visible and sticky
- Step navigation (Continue/Back) works
- `npm run lint` passes
</verification>

<success_criteria>
1. StepIndicator component shows Shipping/Payment/Review with correct states
2. shadcn radio-group installed and used for shipping method
3. Shipping form uses grid layout with labeled inputs
4. Payment form has card fields
5. Order sidebar shows items + pricing summary
6. All checkout logic preserved (no functional changes)
7. Biome lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-update-ui-based-on-pencil-new-pen-design-with-shadcn/25.1-06-SUMMARY.md`
</output>
