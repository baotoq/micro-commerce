---
phase: 20-integration-testing-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/MicroCommerce.ApiService.Tests/Integration/Fixtures/ApiWebApplicationFactory.cs
  - src/MicroCommerce.ApiService.Tests/Integration/Fixtures/FakeAuthenticationHandler.cs
  - src/MicroCommerce.ApiService.Tests/Integration/Fixtures/IntegrationTestBase.cs
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "All 29 existing integration tests pass (MassTransit health check bug fixed)"
    - "Authenticated endpoints can be tested without real Keycloak (FakeAuthenticationHandler injects claims from X-Test-UserId header)"
    - "Test classes get fresh database state via EnsureDeleted/EnsureCreated per class (not MigrateAsync)"
    - "IntegrationTestBase provides CreateAuthenticatedClient, CreateGuestClient, and CreateScope helpers"
    - "All 8 DbContexts are registered in the factory (including ProfilesDbContext, ReviewsDbContext, WishlistsDbContext)"
  artifacts:
    - path: "src/MicroCommerce.ApiService.Tests/Integration/Fixtures/ApiWebApplicationFactory.cs"
      provides: "Fixed factory with all 8 DbContexts, MassTransit health check dedup, EnsureCreated, fake auth, avatar image stub"
      contains: "HealthCheckRegistration"
    - path: "src/MicroCommerce.ApiService.Tests/Integration/Fixtures/FakeAuthenticationHandler.cs"
      provides: "Fake auth handler bypassing Keycloak"
      contains: "X-Test-UserId"
    - path: "src/MicroCommerce.ApiService.Tests/Integration/Fixtures/IntegrationTestBase.cs"
      provides: "Base class with HttpClient helpers and DB scope access"
      contains: "CreateAuthenticatedClient"
  key_links:
    - from: "src/MicroCommerce.ApiService.Tests/Integration/Fixtures/ApiWebApplicationFactory.cs"
      to: "FakeAuthenticationHandler"
      via: "AddAuthentication + AddScheme registration"
      pattern: "AddScheme.*FakeAuthenticationHandler"
    - from: "src/MicroCommerce.ApiService.Tests/Integration/Fixtures/IntegrationTestBase.cs"
      to: "ApiWebApplicationFactory"
      via: "constructor injection from xUnit collection fixture"
      pattern: "IntegrationTestBase.*ApiWebApplicationFactory"
---

<objective>
Fix the existing ApiWebApplicationFactory (MassTransit health check duplicate bug, MigrateAsync -> EnsureCreated, add 3 missing DbContexts), add FakeAuthenticationHandler for Keycloak bypass, and create IntegrationTestBase with authenticated/guest client helpers.

Purpose: Establish working test infrastructure so all existing tests pass and new authenticated feature tests can be written.
Output: Fixed factory, FakeAuthenticationHandler, IntegrationTestBase — all 29 existing integration tests green.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-integration-testing-infrastructure/20-CONTEXT.md
@.planning/phases/20-integration-testing-infrastructure/20-RESEARCH.md
@src/MicroCommerce.ApiService.Tests/Integration/Fixtures/ApiWebApplicationFactory.cs
@src/MicroCommerce.ApiService.Tests/Integration/Fixtures/IntegrationTestCollection.cs
@src/MicroCommerce.ApiService.Tests/Integration/Catalog/CatalogEndpointsTests.cs
@src/MicroCommerce.ApiService.Tests/Integration/Cart/CartEndpointsTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ApiWebApplicationFactory and add FakeAuthenticationHandler</name>
  <files>
    src/MicroCommerce.ApiService.Tests/Integration/Fixtures/ApiWebApplicationFactory.cs
    src/MicroCommerce.ApiService.Tests/Integration/Fixtures/FakeAuthenticationHandler.cs
  </files>
  <action>
  **Fix ApiWebApplicationFactory.cs — 5 changes:**

  1. **Fix MassTransit health check duplicate bug (CRITICAL)**. Before `AddMassTransitTestHarness`, remove all `HealthCheckRegistration` service descriptors whose name starts with `"masstransit"`. This fixes the `System.ArgumentException: Duplicate health checks were registered with the name(s): masstransit-bus` that causes all 29 tests to fail. Use:
     ```csharp
     // Add using: Microsoft.Extensions.Diagnostics.HealthChecks
     var masstransitHealthChecks = services
         .Where(d => d.ServiceType == typeof(HealthCheckRegistration)
             && d.ImplementationInstance is HealthCheckRegistration hcr
             && hcr.Name.StartsWith("masstransit", StringComparison.OrdinalIgnoreCase))
         .ToList();
     foreach (ServiceDescriptor registration in masstransitHealthChecks) services.Remove(registration);
     ```

  2. **Add 3 missing DbContext registrations** (ProfilesDbContext, ReviewsDbContext, WishlistsDbContext) using the same `RemoveAll<DbContextOptions<T>>` + `AddDbContext<T>` pattern as the existing 5 DbContexts. Use schemas: `profiles`, `reviews`, `wishlists` respectively. Add using statements for the three feature infrastructure namespaces.

  3. **Replace MigrateAsync with EnsureCreated in InitializeAsync** — Change `InitializeAsync` to ONLY start the container (`await _dbContainer.StartAsync()`). Remove ALL `MigrateAsync` calls. Schema creation will be handled per-test-class via IntegrationTestBase. This achieves per-test-class database isolation per CONTEXT.md.

  4. **Add FakeAuthenticationHandler registration** in `ConfigureWebHost`. After the MassTransit test harness setup, add:
     ```csharp
     services.AddAuthentication(FakeAuthenticationHandler.SchemeName)
         .AddScheme<AuthenticationSchemeOptions, FakeAuthenticationHandler>(
             FakeAuthenticationHandler.SchemeName, options => { });
     ```
     Add using: `Microsoft.AspNetCore.Authentication`

  5. **Add NoOpAvatarImageService stub** alongside the existing NoOpImageUploadService. The Profiles feature has `IAvatarImageService` for avatar uploads. Stub it with a no-op:
     ```csharp
     services.RemoveAll<IAvatarImageService>();
     services.AddScoped<IAvatarImageService, NoOpAvatarImageService>();
     ```
     Create `NoOpAvatarImageService` as an internal class in the same file (same pattern as `NoOpImageUploadService`). Check `IAvatarImageService` interface methods and implement them returning fake URLs or Task.CompletedTask. Add using for `MicroCommerce.ApiService.Features.Profiles.Infrastructure`.

  **Create FakeAuthenticationHandler.cs:**
  Create `src/MicroCommerce.ApiService.Tests/Integration/Fixtures/FakeAuthenticationHandler.cs` implementing `AuthenticationHandler<AuthenticationSchemeOptions>` using primary constructor syntax. Constant `SchemeName = "Test"`. In `HandleAuthenticateAsync`: check for `X-Test-UserId` header. If missing, return `AuthenticateResult.NoResult()`. If present, create claims array with `ClaimTypes.NameIdentifier` and `"sub"` both set to the userId value. Return `AuthenticateResult.Success(ticket)`. Use file-scoped namespace `MicroCommerce.ApiService.Tests.Integration.Fixtures`.

  **C# conventions to follow:** File-scoped namespaces, primary constructors, explicit types (no `var`), `sealed` on the handler class, collection expressions `[]` for arrays.
  </action>
  <verify>
    <automated>dotnet build src/MicroCommerce.ApiService.Tests/MicroCommerce.ApiService.Tests.csproj --no-restore 2>&1 | tail -5</automated>
    <manual>Verify no build errors; the factory should compile with all 8 DbContexts, health check dedup, and fake auth handler</manual>
  </verify>
  <done>
    - ApiWebApplicationFactory compiles with all fixes (health check dedup, 8 DbContexts, EnsureCreated, fake auth, avatar stub)
    - FakeAuthenticationHandler.cs exists and compiles
    - Build succeeds with no warnings (TreatWarningsAsErrors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create IntegrationTestBase and verify all existing tests pass</name>
  <files>
    src/MicroCommerce.ApiService.Tests/Integration/Fixtures/IntegrationTestBase.cs
  </files>
  <action>
  **Create IntegrationTestBase.cs:**

  Create `src/MicroCommerce.ApiService.Tests/Integration/Fixtures/IntegrationTestBase.cs`:

  - File-scoped namespace `MicroCommerce.ApiService.Tests.Integration.Fixtures`
  - `[Collection("Integration Tests")]` attribute on the class
  - `public abstract class IntegrationTestBase : IAsyncLifetime`
  - Primary constructor taking `ApiWebApplicationFactory factory`
  - Store factory as `protected readonly ApiWebApplicationFactory Factory`

  **Helper methods:**

  1. `protected HttpClient CreateAuthenticatedClient(Guid userId)` — calls `Factory.CreateClient()`, adds `X-Test-UserId` header with userId.ToString(), returns client.

  2. `protected HttpClient CreateGuestClient(Guid? buyerId = null)` — calls `Factory.CreateClient()`. If buyerId has value, adds `Cookie` header with `buyer_id={buyerId.Value}`. Returns client.

  3. `protected IServiceScope CreateScope()` — returns `Factory.Services.CreateScope()`.

  4. `protected async Task ResetDatabase(params Type[] dbContextTypes)` — helper that takes DbContext types, creates a scope, resolves each via `scope.ServiceProvider.GetRequiredService(type)`, casts to `DbContext`, calls `EnsureDeletedAsync()` then `EnsureCreatedAsync()` on each. This standardizes per-test-class DB isolation.

  **IAsyncLifetime:**
  - `public virtual Task InitializeAsync() => Task.CompletedTask;`
  - `public virtual Task DisposeAsync() => Task.CompletedTask;`

  Subclasses override `InitializeAsync` to call `ResetDatabase(typeof(CatalogDbContext), ...)` for the DbContexts they need.

  **Note:** The existing 4 test classes (Catalog, Cart, Ordering, Inventory) do NOT use IntegrationTestBase yet — they inject `ApiWebApplicationFactory` directly. They will still pass because the factory now uses EnsureCreated-per-class pattern (the existing tests don't override InitializeAsync so they share the container DB). The existing tests are NOT modified in this plan — they work as-is since they were designed to create their own data. Future tests (Plan 02) will use IntegrationTestBase for proper DB isolation.

  **After creating the file, run all tests** to verify the factory fix resolves the MassTransit health check bug and all 29 existing tests pass.

  **C# conventions:** explicit types, file-scoped namespace, primary constructors, `[]` collection expressions, sealed where appropriate (but NOT sealed here since it's abstract).
  </action>
  <verify>
    <automated>dotnet test src/MicroCommerce.ApiService.Tests/MicroCommerce.ApiService.Tests.csproj --filter "Category=Integration" --no-build --verbosity normal 2>&1 | tail -20</automated>
    <manual>All 29 existing integration tests should pass. Look for "Passed!" in the output with 29 passed, 0 failed.</manual>
  </verify>
  <done>
    - IntegrationTestBase.cs exists with CreateAuthenticatedClient, CreateGuestClient, CreateScope, ResetDatabase helpers
    - All 29 existing integration tests pass (Catalog 8, Cart 7, Ordering 8, Inventory 6)
    - No new warnings or errors
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/MicroCommerce.ApiService.Tests/` succeeds with no errors or warnings
2. `dotnet test src/MicroCommerce.ApiService.Tests/ --filter "Category=Integration"` shows 29 passed, 0 failed
3. FakeAuthenticationHandler.cs exists in Integration/Fixtures/
4. IntegrationTestBase.cs exists in Integration/Fixtures/
5. ApiWebApplicationFactory.cs contains HealthCheckRegistration removal code
6. ApiWebApplicationFactory.cs registers all 8 DbContexts (OutboxDbContext, CatalogDbContext, CartDbContext, OrderingDbContext, InventoryDbContext, ProfilesDbContext, ReviewsDbContext, WishlistsDbContext)
7. ApiWebApplicationFactory.cs has NO MigrateAsync calls
</verification>

<success_criteria>
- All 29 pre-existing integration tests pass
- Factory registers 8 DbContexts + fake auth + MassTransit test harness + image service stubs
- FakeAuthenticationHandler provides X-Test-UserId header-based claim injection
- IntegrationTestBase provides CreateAuthenticatedClient/CreateGuestClient/CreateScope/ResetDatabase helpers
- Build produces zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/20-integration-testing-infrastructure/20-01-SUMMARY.md`
</output>
