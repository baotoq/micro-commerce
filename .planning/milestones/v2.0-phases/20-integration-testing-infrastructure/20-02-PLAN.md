---
phase: 20-integration-testing-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/MicroCommerce.ApiService.Tests/Integration/Builders/ProductBuilder.cs
  - src/MicroCommerce.ApiService.Tests/Integration/Builders/OrderBuilder.cs
  - src/MicroCommerce.ApiService.Tests/Integration/Profiles/ProfilesEndpointsTests.cs
  - src/MicroCommerce.ApiService.Tests/Integration/Reviews/ReviewsEndpointsTests.cs
  - src/MicroCommerce.ApiService.Tests/Integration/Wishlists/WishlistsEndpointsTests.cs
  - src/MicroCommerce.ApiService.Tests/Integration/Ordering/UpdateOrderStatusHandlerTests.cs
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "One representative integration test exists per feature — 7 features total (Catalog, Cart, Ordering, Inventory, Profiles, Reviews, Wishlists)"
    - "Profiles test uses CreateAuthenticatedClient and verifies auto-created profile for new user"
    - "Reviews test uses public endpoint (GET /api/reviews/products/{productId}) without auth"
    - "Wishlists test uses CreateAuthenticatedClient and verifies empty wishlist for new user"
    - "Handler-level test demonstrates business rule testing pattern with UpdateOrderStatusCommandHandler"
    - "Test data builders (ProductBuilder, OrderBuilder) provide fluent API for readable test data creation"
    - "All integration tests pass including existing 29 and new tests"
  artifacts:
    - path: "src/MicroCommerce.ApiService.Tests/Integration/Profiles/ProfilesEndpointsTests.cs"
      provides: "Profiles feature representative test"
      contains: "CreateAuthenticatedClient"
    - path: "src/MicroCommerce.ApiService.Tests/Integration/Reviews/ReviewsEndpointsTests.cs"
      provides: "Reviews feature representative test"
      contains: "GetProductReviews"
    - path: "src/MicroCommerce.ApiService.Tests/Integration/Wishlists/WishlistsEndpointsTests.cs"
      provides: "Wishlists feature representative test"
      contains: "CreateAuthenticatedClient"
    - path: "src/MicroCommerce.ApiService.Tests/Integration/Ordering/UpdateOrderStatusHandlerTests.cs"
      provides: "Handler-level test for business rule edge case"
      contains: "UpdateOrderStatusCommand"
    - path: "src/MicroCommerce.ApiService.Tests/Integration/Builders/ProductBuilder.cs"
      provides: "Fluent builder for Product test data"
      contains: "ProductBuilder"
    - path: "src/MicroCommerce.ApiService.Tests/Integration/Builders/OrderBuilder.cs"
      provides: "Fluent builder for Order test data"
      contains: "OrderBuilder"
  key_links:
    - from: "src/MicroCommerce.ApiService.Tests/Integration/Profiles/ProfilesEndpointsTests.cs"
      to: "IntegrationTestBase"
      via: "inherits base class for auth helpers and DB reset"
      pattern: "IntegrationTestBase"
    - from: "src/MicroCommerce.ApiService.Tests/Integration/Ordering/UpdateOrderStatusHandlerTests.cs"
      to: "OrderingDbContext"
      via: "direct DbContext access for handler-level test"
      pattern: "OrderingDbContext"
---

<objective>
Add the 3 missing feature integration tests (Profiles, Reviews, Wishlists), one handler-level test (UpdateOrderStatusCommandHandler), and test data builders (ProductBuilder, OrderBuilder) to complete TEST-01 coverage.

Purpose: Prove the integration test pattern across all 7 features and demonstrate both API endpoint and handler-level testing patterns.
Output: 3 new endpoint test classes + 1 handler test class + 2 test data builders. All tests green.
</objective>

<execution_context>
@/Users/baotoq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/baotoq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-integration-testing-infrastructure/20-CONTEXT.md
@.planning/phases/20-integration-testing-infrastructure/20-RESEARCH.md
@.planning/phases/20-integration-testing-infrastructure/20-01-SUMMARY.md
@src/MicroCommerce.ApiService.Tests/Integration/Fixtures/IntegrationTestBase.cs
@src/MicroCommerce.ApiService.Tests/Integration/Fixtures/FakeAuthenticationHandler.cs
@src/MicroCommerce.ApiService/Features/Profiles/ProfilesEndpoints.cs
@src/MicroCommerce.ApiService/Features/Reviews/ReviewsEndpoints.cs
@src/MicroCommerce.ApiService/Features/Wishlists/WishlistsEndpoints.cs
@src/MicroCommerce.ApiService/Features/Ordering/Application/Commands/UpdateOrderStatus/UpdateOrderStatusCommandHandler.cs
@src/MicroCommerce.ApiService/Features/Ordering/Domain/Entities/Order.cs
@src/MicroCommerce.ApiService/Features/Catalog/Domain/Entities/Product.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test data builders and 3 feature endpoint tests</name>
  <files>
    src/MicroCommerce.ApiService.Tests/Integration/Builders/ProductBuilder.cs
    src/MicroCommerce.ApiService.Tests/Integration/Builders/OrderBuilder.cs
    src/MicroCommerce.ApiService.Tests/Integration/Profiles/ProfilesEndpointsTests.cs
    src/MicroCommerce.ApiService.Tests/Integration/Reviews/ReviewsEndpointsTests.cs
    src/MicroCommerce.ApiService.Tests/Integration/Wishlists/WishlistsEndpointsTests.cs
  </files>
  <action>
  **Create test data builders:**

  1. **ProductBuilder.cs** (`Integration/Builders/ProductBuilder.cs`):
     - File-scoped namespace `MicroCommerce.ApiService.Tests.Integration.Builders`
     - `public sealed class ProductBuilder` with fluent API
     - Default values: Name="Test Product", Description="Test Description", Price=99.99m, CategoryId=Guid.NewGuid()
     - Methods: `WithName(string)`, `WithDescription(string)`, `WithPrice(decimal)`, `WithCategoryId(Guid)`
     - `Build()` returns a `Product` entity using `Product.Create(...)` factory method. Read the Product entity class to determine the correct factory method signature (Product.Create likely takes name, description, price, categoryId, imageUrl, sku).
     - Use explicit types, no `var`

  2. **OrderBuilder.cs** (`Integration/Builders/OrderBuilder.cs`):
     - File-scoped namespace `MicroCommerce.ApiService.Tests.Integration.Builders`
     - `public sealed class OrderBuilder` with fluent API
     - Default values: BuyerId=Guid.NewGuid(), appropriate defaults for order items
     - Methods: `WithBuyerId(Guid)`, potentially `WithItem(...)`
     - `Build()` returns an `Order` entity. Read the Order entity to understand the correct factory method (likely `Order.Create(buyerId, items)` or similar). The builder needs to produce an Order in "Submitted" status (the initial state).
     - Use explicit types, no `var`

  **Create ProfilesEndpointsTests.cs** (`Integration/Profiles/ProfilesEndpointsTests.cs`):
  - Inherit from `IntegrationTestBase`
  - `[Trait("Category", "Integration")]` attribute
  - Override `InitializeAsync` to call `ResetDatabase(typeof(ProfilesDbContext))` — read `ProfilesDbContext` to get the correct namespace/import
  - Representative test: `GetMyProfile_NewUser_AutoCreatesAndReturnsProfile`
    - Create authenticated client with random userId
    - GET `/api/profiles/me`
    - Assert 200 OK, profile returned with matching userId
  - Read ProfilesEndpoints.cs to determine exact route paths and response DTO types. The GetProfile endpoint likely auto-creates a profile for a new user (via CreateProfileCommand inside GetProfileQuery handler).

  **Create ReviewsEndpointsTests.cs** (`Integration/Reviews/ReviewsEndpointsTests.cs`):
  - Inherit from `IntegrationTestBase`
  - `[Trait("Category", "Integration")]` attribute
  - Override `InitializeAsync` to call `ResetDatabase(typeof(ReviewsDbContext))`
  - Representative test: `GetProductReviews_NoReviews_ReturnsEmptyList`
    - Use `CreateGuestClient()` — this is a public endpoint (no auth required)
    - GET `/api/reviews/products/{productId}` with random productId
    - Assert response is OK with empty items list and TotalCount 0
  - Read ReviewsEndpoints.cs to determine exact route path and response DTO shape.

  **Create WishlistsEndpointsTests.cs** (`Integration/Wishlists/WishlistsEndpointsTests.cs`):
  - Inherit from `IntegrationTestBase`
  - `[Trait("Category", "Integration")]` attribute
  - Override `InitializeAsync` to call `ResetDatabase(typeof(WishlistsDbContext))`
  - Representative test: `GetWishlist_EmptyWishlist_ReturnsEmptyList`
    - Use `CreateAuthenticatedClient(userId)` — wishlists require auth
    - GET `/api/wishlist`
    - Assert response is OK with empty list
  - Read WishlistsEndpoints.cs to determine exact route path and response DTO shape.

  **C# conventions:** File-scoped namespaces, explicit types (no `var`), `sealed` classes, collection expressions `[]`, primary constructors where appropriate, FluentAssertions `.Should()` for all assertions.
  </action>
  <verify>
    <automated>dotnet test src/MicroCommerce.ApiService.Tests/MicroCommerce.ApiService.Tests.csproj --filter "Category=Integration" --no-restore --verbosity normal 2>&1 | tail -20</automated>
    <manual>Verify 3 new test classes compile and their tests pass. Should see Profiles, Reviews, Wishlists tests in output.</manual>
  </verify>
  <done>
    - ProductBuilder.cs and OrderBuilder.cs exist with fluent APIs
    - ProfilesEndpointsTests.cs passes: authenticated user gets auto-created profile
    - ReviewsEndpointsTests.cs passes: public endpoint returns empty reviews for unknown product
    - WishlistsEndpointsTests.cs passes: authenticated user gets empty wishlist
    - All existing 29 tests still pass (no regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create handler-level test for UpdateOrderStatusCommandHandler</name>
  <files>
    src/MicroCommerce.ApiService.Tests/Integration/Ordering/UpdateOrderStatusHandlerTests.cs
  </files>
  <action>
  **Create UpdateOrderStatusHandlerTests.cs** (`Integration/Ordering/UpdateOrderStatusHandlerTests.cs`):

  - Inherit from `IntegrationTestBase`
  - `[Trait("Category", "Integration")]` attribute
  - Override `InitializeAsync` to call `ResetDatabase(typeof(OrderingDbContext))`

  **Read the following files first to understand the handler contract:**
  - `src/MicroCommerce.ApiService/Features/Ordering/Application/Commands/UpdateOrderStatus/UpdateOrderStatusCommandHandler.cs` — understand the handler's `Handle` method, what command it takes, what it returns (likely `Result` from FluentResults)
  - `src/MicroCommerce.ApiService/Features/Ordering/Domain/Entities/Order.cs` — understand Order factory method, status transitions (OrderStatus SmartEnum), which transitions are invalid

  **Test method: `Handle_InvalidStatusTransition_ReturnsFailResult`**
  - This tests the business rule that certain status transitions are invalid (e.g., cannot ship an order that hasn't been confirmed)
  - Arrange: Create an Order in Submitted status directly in the DB using `OrderingDbContext`
    - Use OrderBuilder to create the Order entity
    - Add to `db.Orders` and `SaveChangesAsync`
  - Act: Resolve the handler from DI (or create it with the DbContext). Call `Handle(new UpdateOrderStatusCommand(orderId, "Shipped"), CancellationToken.None)`.
    - Read the actual command record to get exact parameter names
    - If the handler is registered in DI, resolve via `IMediator.Send()` or resolve the handler directly
  - Assert: `result.IsFailed.Should().BeTrue()` — the business rule prevents shipping a Submitted order (must go through Confirmed first)
    - Also assert the error message contains relevant text about the invalid transition

  **Test method: `Handle_NonExistentOrder_ReturnsFailResult` (or throws NotFoundException)**
  - Arrange: Use a random Guid for orderId
  - Act: Call handler with non-existent orderId
  - Assert: Result is failed (or NotFoundException is thrown — check the handler implementation)

  **Important:** The handler uses `Result` return type from FluentResults (Phase 17). The test should use `result.IsFailed.Should().BeTrue()` and `result.Errors` to verify business rule violations. Use FluentAssertions for all assertions.

  **C# conventions:** File-scoped namespace, explicit types, sealed class, FluentAssertions, no `var`.
  </action>
  <verify>
    <automated>dotnet test src/MicroCommerce.ApiService.Tests/MicroCommerce.ApiService.Tests.csproj --filter "Category=Integration" --no-restore --verbosity normal 2>&1 | tail -20</automated>
    <manual>Verify handler-level tests pass. Should see UpdateOrderStatusHandlerTests in output. Total test count should be 29 + new tests.</manual>
  </verify>
  <done>
    - UpdateOrderStatusHandlerTests.cs exists with at least 2 handler-level tests (invalid transition + non-existent order)
    - Handler tests demonstrate: direct DB seeding, handler invocation, FluentResults assertion pattern
    - All integration tests pass (existing 29 + all new feature tests + handler tests)
    - Zero build warnings
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/MicroCommerce.ApiService.Tests/` succeeds with zero warnings
2. `dotnet test src/MicroCommerce.ApiService.Tests/ --filter "Category=Integration"` shows all tests passed (29 existing + new tests)
3. Test coverage per feature: Catalog (existing), Cart (existing), Ordering (existing + handler-level), Inventory (existing), Profiles (new), Reviews (new), Wishlists (new)
4. Handler-level test demonstrates FluentResults assertion pattern (result.IsFailed, result.Errors)
5. Test data builders exist: ProductBuilder.cs, OrderBuilder.cs
6. All 3 new test classes inherit from IntegrationTestBase and use its helpers
</verification>

<success_criteria>
- 7 features have at least one representative integration test each
- Handler-level test proves the business rule testing pattern with FluentResults
- Test data builders provide fluent API for Product and Order creation
- All tests pass with per-test-class database isolation
- Zero build warnings
</success_criteria>

<output>
After completion, create `.planning/phases/20-integration-testing-infrastructure/20-02-SUMMARY.md`
</output>
