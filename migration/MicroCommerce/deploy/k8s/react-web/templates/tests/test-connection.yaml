apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "react-web.fullname" . }}-test-connection"
  labels:
    {{- include "react-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "react-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
